# Contributing

Create a focused branch, read the current state and relevant policies, and keep protected control changes separate from strategy changes. Add the smallest test that proves new behavior and a failure path. Run every command in `README.md`. Update persistent documentation when behavior, policy, or the next task changes. Never commit runtime datasets, credentials, generated reports, or broker responses.
