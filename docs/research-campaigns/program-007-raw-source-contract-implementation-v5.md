# Program 007 raw source contract implementation v5

## Status

Program 007, `multi-hour-sector-etf-research-006`, remains
`PROPOSED-NOT-AUTHORIZED`. Its raw-first implementation is synthetic-only. Dataset admission,
ledger-backed normalization, and any one-use authority proposal remain blocked because ledger v2
leaves IWM, MDY, and SPY `COVERAGE-UNRESOLVED`.

V4 review was not finding-free. Exact `bytes` subclasses could execute overridden methods while
loading proposal or response data, and an internal symbolic link could redirect retained artifacts
outside the source-owned temporary workspace. V5 supersedes V4 without changing the frozen sample,
ledger, source parameters, science, chronology, costs, delays, budgets, or authority state.

## Corrected execution boundary

- Proposal bytes, response bodies, and response statuses must be exact built-in values before any
  operation that can invoke their methods.
- The same exact-body rule is enforced again at the shared response-budget and JSON boundaries.
- Every used restart, request, page, receipt, validation, lock, and manifest path is checked against
  the owned temporary root before reading or writing.
- Existing symbolic links in the used path are rejected, and final file opens use `O_NOFOLLOW`.
- The public entry still accepts only the exact finite `SyntheticPageSource`; it has no provider
  client, credential API, storage-root parameter, callable input, or real transport.
- Request intent still precedes response consumption. Restart still uses retained pages without
  consuming another synthetic response.

A future real transport must use a separate implementation bound to an exact active one-use
authority and ignored private root. It must not extend this synthetic entry point.

## Unchanged evidence

Ledger v2 resolves ten symbols. XLB, XLE, XLK, XLU, and XLY each have a 2-for-1 split effective
`2025-12-05`; XLF, XLI, XLP, XLRE, and XLV have bounded no-applicable-action conclusions. IWM, MDY,
and SPY remain unresolved, which blocks the whole thirteen-symbol dataset and ledger-backed
normalization.

The unchanged six-chain sample produces 14,742 canonical RTH coordinates across fifteen XNYS
sessions in seven synthetic responses. Valid extended-hours rows remain raw-only. Invalid rows fail
after bounded retention. Raw prices remain unadjusted, and exact split ratios apply only to
cross-session relative-volume share counts.

## Immutable binding

- Source commit: `a8345c5d3379cd274a02f7239c3698003a19585b`
- Implementation root: `9b0d4991ea6ed172ee52746b7aa18777560279384f37e64dd51d63d6ea828e64`
- V5 artifact SHA-256: `a5c52a4c2ce58618f5a4708d883c38d17b28a5b0b088d4b1702fe20a02d4f1e7`
- V5 fingerprint: `052bc7f2b7b900b64d3fa6ecea64cbc9759ac49640ea39fbdea2db0ae34a597c`
- Ledger v2 SHA-256: `3b815581d0da66db427243bce34f9ced5021f73719acd2b5d5e277d57065d53a`
- Ledger v2 fingerprint: `0ec39d6f38d469e099862173ff710c0e737b39b464e233e291c9e9b20c089c25`

Implementation artifacts and reports v1 through v4 remain byte-identical history. V4 is not a
finding-free implementation claim.

## Verification and next gate

Thirty-eight Program 007 synthetic and forensic tests, Ruff, strict mypy, and the sanitized full
suite pass; the full result is 1,327 passed and four skipped. The final public secret scan, complete
repository gates, build, runtime manifest, fresh independent review, CI, and post-merge provenance
remain required.

No Alpaca or market-data provider request, credential value or presence read, Program 006 private
page read, strategy calculation, controlled or protected access, PAPER action, broker write, or live
action occurred. Every authority flag remains false.

The next gate is complete public issuer or exchange unit-changing-action coverage for IWM, MDY, and
SPY in a new strict ledger version, followed by independent review, green CI, and clean synchronized
main. Only then may a separate one-use raw Alpaca SIP structural source-qualification authority
proposal be drafted and reviewed for a new explicit user grant. This work does not create,
authorize, or execute that proposal.
