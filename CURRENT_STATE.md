# Current state

- Milestone: M4 injected-only paper cancellation adapter; no candidate qualified.
- Completed: all prior capabilities plus immutable execution intents, code-bound paper authorization, independent risk gates, default-on emergency disable, durable rejected risk decisions, journaled normalized and adapter-attested paper snapshots with prior-close equity, derived account daily PnL evidence, stable emergency-clear readiness, idempotent journaled emergency transitions, atomic immutable capacity reservations for approved risk decisions, exact temporal active-reservation-set provenance bound into risk-context fingerprints, deterministic broker-free long-only order deltas, verified reservation-bound staged orders, persisted forward-only local order lifecycle states with atomic single-submitter claims, journaled capacity release after zero-fill cancellation or rejection, normalized immutable broker-event evidence with exact deduplication, cumulative quantity and average-fill-price checks, baseline-bound immutable expected-position advances, immutable adapter-attested position-settlement proof, read-only settlement-capacity assessment, production-attested IEX quote and NYSE clock evidence, deterministic IEX-ask long-exposure valuation, side-aware bid/ask execution-price risk checks, sequence checks, atomic local-state application, fail-closed emergency disable on conflicts, a read-only journal-verified list of submission-unknown orders requiring recovery, immutable sanitized exact-lookup 404 evidence from the production paper reader, a read-only proof that binds one 404 to later complete clean reconciliation and unchanged protected controls, and atomic release of exclusive positive-fill reservations only after the current transaction-bound attested context matches their settlement proof. Readiness uses the latest three distinct evidence records for one baseline, requires each to be clean and separated by the explicit reviewed stability interval in `RiskLimits`, rechecks current freshness and active exact authorization and limits, and resets after a dirty latest sample. Clear recomputes readiness inside one immediate transaction and dirty reconciliation atomically restores disable. Approved decisions now reserve cash, gross exposure, order notional, and order count in the same transaction. No holdout or paper authorization exists for the current rejected candidates.
- Work in progress: quantity-target submission has transaction-bound preflight, one-shot fake outcomes, and strict injected-only Alpaca POST normalization. Cancellation has separate immutable attempt and unknown-outcome evidence, a one-shot fake coordinator, and a strict injected-only `DELETE /v2/orders/{broker_order_id}` adapter. Successful acceptance remains unresolved until a later terminal broker event; timeout records unknown and replay never calls again. Broker fill state stays authoritative throughout. Weight-target submission remains blocked pending a reviewed share-rounding rule. Reviewed limits still have no production values. Adapter provenance is a local code-path control, not a cryptographic claim against hostile local code or filesystem access. No active risk-limit file, production HTTP broker transport, paper CLI, or real broker mutation exists.
- Known limitations: the relative-strength family will not proceed to validation. Its best training result was the 168-session neighbor with 0.025127 total return, 0.161472 Sharpe, 0.150061 maximum drawdown, and 45 trades. The risk-managed momentum base lost 0.063431 with a -0.229125 Sharpe ratio. The volatility-balanced base lost 0.034052 with a -0.050968 Sharpe ratio, 0.239707 maximum drawdown, and 0.636804 top-instrument profit share. Fixed-weight beat both earlier short-horizon trend baselines in every validation year, and the rejected long-horizon momentum family lost money across all training parameters. Alpaca IEX common coverage begins on 2020-07-27; the installed XNYS calendar begins on 2006-08-02. The 2023–2026 data remains untouched by all four training-only campaigns, and the 2026 segment remains untouched by all research. There is no qualified candidate, broker, or execution system.
- Test status: 141 tests pass; ruff format and lint, strict mypy, secret scan, and diff checks pass on Python 3.12.13. Approved passing controlled evidence can authorize one exact holdout or separately reviewed paper record; manual, legacy, unbound, tampered, or approved failing evidence cannot. Live and broker-write modes remain rejected.
- Safety: defaults offline; live execution and broker submission are absent.
- Next task: add deterministic cancel-all planning and per-order attempt evidence without a bulk broker endpoint; live writes and production paper calls remain blocked.
- Branch: `codex/m4-injected-paper-cancel`.
