# Current state

- Branch-only intraday campaign note: `intraday-research-v1` is preregistered at plan fingerprint `ce81be36d02cc15f421390bf3d3787714bb0b025797ccfb8de2c1d1236052c1a` against reviewed M5B commit `b1774f547da2976348430b820faf2ebdacdf46af`. It fixes SPY/QQQ `5m` data, about six training months, three following two-month validation windows, the three M5B baselines, base/10+2/20+5 bps costs, one/two/three-bar delays, no parameter neighbors, and 60 candidate ordinals. The plan is atomically sealed in this worktree's isolated official registry; arbitrary `run-intraday` candidates cannot enter the sealed campaign, and the planned runner derives every result-affecting field from stored reservations after exact Alpaca dataset-manifest checks. The inspector creates no runtime state, and neither path grants holdout, paper, broker-write, or live authority. This isolated worktree has no local `.env` or nonempty Alpaca historical credential variables, so no real dataset or real candidate ran. An ignored deterministic-fixture proof retained one failed incomplete-session run, accounted for its six-record budget, and reproduced one research-gate artifact; fixture outcomes are not campaign evidence.

- Branch-only intraday note: `codex/intraday-m5b` adds `intraday-experiment-v1`, controlled exact-range training and validation, `intraday-backtest-report-v1`, fixed cash/previous-bar-momentum/12-bar-trend SPY/QQQ baselines, and research-only `intraday-qualification-policy-v1` on top of the offline `1m` and `5m` replay foundation. Every baseline uses `XNYS-regular-session-flat-v1`; normal and early-close sessions end flat, cost and exact `+1`/`+2` whole-bar delay variants remain linked to one frozen parent, and failed named sources remain content-addressed registry evidence. The assessment CLI requires the committed v1 policy fingerprint. Protected intraday holdout, paper, broker-write, and live authority remain absent. The active M5 runtime described below is unchanged.

- Milestone: M4 strategic-allocation entry completed; the first M5 sustained-observation campaign is active and action-plan equivalence is under review.
- Completed: all prior M0–M3 capabilities plus dormant M4 paper controls: immutable intents and paper authorization, independent transaction-bound risk gates and capacity reservations, default-on emergency disable, append-only journal evidence, normalized paper snapshots and broker events, reconciliation and stable emergency-clear readiness, conservative whole-share target planning, deterministic order staging and forward-only lifecycle state, one-shot submission and cancellation evidence, recovery proofs, fixed-origin production market/account reads, runtime activation and process opt-in, main-only attested builds, exact non-editable installed-distribution verification, fresh runtime identity bound into paper-attempt transactions, and historical flat settlement/checkpoint verification that remains valid after the first later reservation. Strategic allocation passed all 17 approved validation gates and all seven approved one-time holdout gates.
- Work in progress: the paper account holds 4 SPY at `768.445`, 3 QQQ at `720.53`, 8 IWM at `301.37`, and 3 GLD at `375.32`; cash is `$91,227.71`, no broker order remains open, every fill has exact lookup, settlement, strategy-equity, and capacity-release evidence, and emergency generation 4 remains clear. The planned activation is revoked and process opt-in is blank. Campaign `paper-week-1-20260804` runs through `2026-08-11T17:26:24.825416Z` with a 900-second maximum gap. Comparison `initial-entry-v1` found the replay, shadow, and four stored paper actions equivalent; its record fingerprint is `17e11c959ea45635f43db54c2c4d337adfe3c6bd48fc29105ff8eeed1127bec0`. The Windows task remains the active writer. The current change adds an alternative locked 10-minute GNU Screen runner for a Linux VPS plus dry-run-first, repository-scoped cleanup; migration has not started. Build-provenance run `30935026330` and the installed runtime both verify commit `1f1fb374d3c52272e3843de950780440f685b8c7`.
- Known limitations: the relative-strength, long-horizon momentum, risk-managed momentum, and volatility-balanced training families were rejected. The strategic-allocation base returned 0.141474 in training and between 0.231086 and 0.280751 across three validation years; its worst validation Sharpe was 1.272427 and maximum drawdown was 0.177431. Its 2026 holdout returned 0.091569 with a 0.990546 Sharpe ratio and 0.107254 maximum drawdown; all approved holdout gates passed. These backtests do not establish future profitability. Alpaca IEX common coverage begins on 2020-07-27; the installed XNYS calendar begins on 2006-08-02. Paper authorization and activation expire within 24 hours, and every source-commit change requires a new attested installed build and replacement activation. No sustained execution system exists.
- Test status: 227 tests pass; CI also parses every Bash script before building the wheel. Intraday coverage includes same-timestamp causal fills, delayed flat-at-close behavior, incomplete-session and unsafe late-entry rejection, normal and early closes, atomic SPY/QQQ flattening, experiment identity, exact-range runs, zero-trade reports, deterministic metrics, fixed search accounting, failed-source evidence, ordered cost stress, exact `+1`/`+2` delay stress, research-only qualification lineage, strict v1 plan fingerprinting, sealed registry reservations, arbitrary-run rejection, exact dataset binding, and planned-run failure retention. Campaign coverage includes healthy continuity, stale status, sanitized read failure, position drift, exact replay, completion freshness, and tamper rejection. Equivalence coverage binds paper intents, retains target mismatch, and verifies idempotent replay. Approved passing controlled daily evidence can authorize one exact daily holdout or separately reviewed paper record; intraday evidence cannot. Live mode remains rejected; observation and equivalence cannot create process opt-in or bypass transaction-bound controls.
- Safety: defaults offline; live execution and autonomous broker submission are absent.
- Next task: merge the VPS runner and cleanup tools, then migrate only after the VPS SSH target is available and the Windows writer is stopped.
- Delivery: reviewed branches merge to `main`; runtime authority binds the resulting main commit.
