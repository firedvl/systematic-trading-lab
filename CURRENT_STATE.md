# Current state

- Milestone: M4 strategic-allocation entry is complete. The current M5 operational work is closed with an explicit waiver of the remaining 168-hour sustained-observation duration.
- Rapid Research: a separate non-authoritative daily workflow now supports strategy discovery, local CSV/Parquet import, net-of-cost backtests, bounded parameter sweeps, rolling or expanding chronological walk-forward folds, stricter cost/delay stress, score-free comparison, and zero-authority candidate export. It uses `rapid-research.sqlite3` and `rapid-research/` artifacts only. It does not write experiment, qualification, holdout, paper, risk, order, execution, reconciliation, broker, or V3 state. Cataloged and user-supplied reads fail closed on registered or reserved holdout ranges in the active controlled registry, and identifiable controlled catalog artifacts cannot be reimported through the local-data path even from another storage root. Public commands also hard-reject inclusive overlap with all three selected V3 validation windows and expose no override.
- M5 evidence: campaign `paper-week-1-vps-20260804` completed with 1008 healthy observations, no position drift, and one isolated read failure that recovered at the next scheduled sample. Its final state was healthy, but its approximately 1030.755-second maximum gap, reported as 1031 seconds, exceeded the fixed 900-second limit during two orderly VPS reboots. The formal continuity gate therefore failed. A later shorter reboot/recovery validation passed, and the longer follow-up campaign remained healthy through the point when it was stopped. The user waived the unobserved remainder of the 168-hour requirement. The waiver closes the current operational work; it does not turn Week 1 into a pass, prove the unobserved period, or grant broker-write authority.
- Completed platform: all prior M0–M3 capabilities plus dormant M4 paper controls, immutable intents and authorization, transaction-bound risk gates and capacity reservations, default-on emergency disable, append-only evidence, normalized paper snapshots and events, reconciliation and recovery proofs, fixed-origin production reads, runtime activation and process opt-in, main-only attested builds, exact installed-runtime verification, and restart-safe read-only observation supervision. Strategic allocation passed all 17 approved validation gates and all seven approved one-time holdout gates.
- Intraday foundation: research-only `1m` and `5m` SPY/QQQ data, XNYS regular-session validation, completed-bar replay, flat-at-close enforcement, exact-range controlled training and validation, and `intraday-qualification-policy-v1` are implemented. V1/V2 retain `intraday-experiment-v1`, `intraday-backtest-report-v1`, exact-weight execution, and their fixed cash/previous-bar-momentum/12-bar-trend semantics. Intraday holdout, paper, broker-write, and live authority remain absent.
- Campaign V1 disposition: `intraday-research-v1` remains sealed at plan fingerprint `ce81be36d02cc15f421390bf3d3787714bb0b025797ccfb8de2c1d1236052c1a` and foundation reference `b1774f547da2976348430b820faf2ebdacdf46af`. Its source review was recorded. The first real Alpaca Training acquisition then returned the complete expected grid plus 2,758 extended-hours and close-boundary records. Dataset validation rejected the import before publication, with no missing intervals and no duplicates. No candidate ran and no strategy result was observed. The plan, review, runtime state, and quarantine evidence remain unchanged and readable. New V1 sealing, binding, review, qualification, lifecycle mutation, and execution are blocked; additive SQLite triggers reject direct updates and deletes of its campaign, plan, and candidate rows.
- Campaign V2: `intraday-research-v2` completed 60/60 controlled candidates with 60 execution-source bindings and one immutable source review under plan fingerprint `52db8a27fa4ff86865ab69b6bd7456899329ef3b861a582e59ab32904c03c122` and foundation reference `f3d7ee7d86c3a02b52c09270a6399aa1bf5f78b7`. All 12 base research qualification groups failed. The campaign is closed immutable failed evidence. Its reported diagnostic export SHA-256 is `f02cbb3dbb65859e085a2a73f2f062d1a43031acef6862c8a42dd2e47791fdaf`. The postmortem confirms repeated exact-weight scheduling caused drift-driven churn and that pending orders made longer delay variants change accepted target cadence as well as latency. No holdout was accessed or authorized, and no paper, broker-write, or live authority exists.
- Campaign V3 preregistration: development-only `intraday-experiment-v2`, `intraday-backtest-report-v2`, FIFO delayed state-transition execution, exact paired zero-cost diagnostics, fixed event-driven 12-bar MA, six-bar momentum, and six-bar opening-range breakout contracts are implemented. The exposure inventory remains `0666996faabb50abce0b8959c49980e36a655ea290618bc1463342d2ab5122f9`. The replacement selection fingerprint is `c2718c3871bb95e22d4647e119f6bfb54cd51ec7b1b2cc472cfa1a7dfbcfc5d0`, and the final static 60-candidate plan fingerprint is `5e81cf8f0db1143f293a0f93900f1e797718443a559c1caaaa2e986851d5241a`. Training remains exposed. The author-recorded selection date is descriptive, not a trusted cutoff. Validation A/B/C are eligible for prospective market-data freshness, but prospective freshness, validation approval, universal freshness, and every authority remain false until a verified GitHub/main Sigstore transparency-log timestamp for the exact seal becomes the effective selection cutoff and precedes `2026-10-01T13:30:00Z`. Committing the plan creates no runtime state.
- Campaign V3 controls: seal creation and registry verification parse the exact inventory, selection, plan, and qualification binding through their shared strict validators. A known selected-period acquisition recorded before sealing rejects the selection even when the inventory and selection fingerprints are recomputed. The V3-only registry verifies and stores the exact attested seal before atomically creating 60 pending reservations. It resolves four dataset IDs through the shared catalog, validates raw and normalized integrity, and binds all four roles and 60 stored specs atomically. Source review reruns the whole-package artifact assessment and requires the explicitly reviewed fingerprint. The runner accepts only the stored candidate ID, storage root, and fixed source artifacts. Each claim has an unexposed lease token required by heartbeats, source checks, failure, and publication. Publication first commits an immutable intent containing the canonical report, then create-only publishes and directory-syncs the exact bytes before committing completion. Stale recovery atomically takes an intent's lease and finishes it; completed-intent reconciliation restores a missing file, while substituted bytes create immutable integrity-conflict evidence that registered qualification rejects. A running publication conflict leaves terminal failure evidence. Registered qualification derives exact report roles from the immutable stored plan, uses only realistic-cost metrics, and fingerprints all 60 terminal sources. No runtime reservation, dataset binding, source review, result, or qualification evidence exists.
- Known limitations: the relative-strength, long-horizon momentum, risk-managed momentum, and volatility-balanced daily training families were rejected. The strategic-allocation results are historical evidence, not proof of future profitability. V2 did not contain an exact zero-cost replay, and its delay stress was confounded. V3 trusted publication is unattested, so prospective freshness and candidate-period approval remain false and runtime materialization is blocked. The whole-package preassessment cannot become a trusted V3 source review until a reviewed commit merges, GitHub produces the exact main-attested artifacts, and a human records the immutable campaign review. Candidate 1 also cannot run before Validation C completes at `2027-04-15T20:00:00Z` and all four later datasets validate and bind. The caller-configured V1 research path remains separate and cannot produce V3 evidence; using selected validation dates before the trusted seal invalidates this campaign. No V3 dataset has been acquired, no V3 result has been observed, and no V3 qualification has passed. Alpaca IEX common coverage begins on 2020-07-27; the installed XNYS calendar begins on 2006-08-02. Paper authorization and activation expire within 24 hours, and each source-commit change requires a new attested installed build and replacement activation. The M5 duration waiver leaves the unobserved part of the 168-hour period as an explicit evidence gap. No sustained execution system exists.
- Test status: 465 tests pass and four are skipped. Ruff format and lint, strict mypy across 113 source files, the 201-file secret scan, shell syntax, sdist and wheel builds, local documentation-link checks across 42 files, the clean-home newcomer subprocess path, both frozen campaign-plan inspections, V1/V2 golden fingerprints, the immutable V2 manifest, and 204 focused V1/V2/V3 invariant tests pass. The final merged-main GitHub attestation cannot exist before merge.
- Safety: defaults offline; live execution and autonomous broker submission are absent.
- Next research development: after review and merge, use exposed historical daily periods to test broader strategy families through Rapid backtest, sweep, walk-forward, and stress runs. Package promising results only as zero-authority candidates, then design separate controlled evidence and paper evaluation. Do not run a real strategy campaign as part of this infrastructure change.
- V3 next step: obtain and verify the exact GitHub/main seal before Validation A's first bar, then explicitly materialize the 60 reservations. After Validation C ends, independently acquire and validate the four exact datasets, bind them atomically, preassess the attested whole-package source including every added package module, and record the human source review before candidate 1. A missing or late seal, changed design, pre-seal selected-period acquisition, failed dataset, or source mismatch requires rejection or a new versioned selection and plan. Do not run V3 or ordinary research on the selected validation dates before the trusted seal.
- Delivery: reviewed branches merge to `main`; runtime authority binds the resulting main commit.
