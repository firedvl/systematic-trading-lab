# Roadmap

Each milestone requires passing formatting, lint, typing, tests, secret checks, documentation review, and every listed gate. A later milestone does not weaken an earlier gate.

## Immediate research direction

1. Use Rapid Research for fast historical daily strategy development on exposed data.
2. Add and reject broader strategy families through bounded, fully recorded sweeps.
3. Require chronological walk-forward and worse-execution stress before candidate export.
4. Package promising results as zero-authority paper-candidate inputs for separate controlled review.
5. Evaluate only qualified, independently authorized candidates in the guarded paper system.

Research iteration is cheap; promotion and execution remain strict. V3 stays separate: its exact main seal was attested before Validation A, and the campaign has been materialized with 60 pending reservations. It now awaits its future validation periods and does not block ordinary historical research. Live trading remains out of scope.

## M0 — Foundation and policy

Package, safe configuration, CLI, CI, persistent documentation, initial domain types, deterministic fingerprints, storage layout, and test infrastructure. Gate: clean setup; CLI and checks pass; runtime defaults offline; live is rejected; no secrets or generated data are tracked.

## M1 — Market data

Provider-neutral acquisition, Alpaca historical adapter, fixtures, immutable raw and normalized storage, catalog, validation and quarantine, calendar and corporate-action policy, and data CLI. Gate: adjusted daily data for the fixed universe is reproducible; provider corrections create versions; invalid data cannot silently enter a dataset; catalog reconstruction works. The fixed-universe gate now covers XNYS calendar validation, raw JSONL and Parquet artifacts, deterministic fixtures, read-only paginated Alpaca acquisition, immutable correction lineage, cross-provider version isolation, provider-adjusted action handling, rejection of unadjusted data, and issuer-sourced point-in-time membership bound into dataset and experiment provenance. A local unadjusted corporate-action processor remains out of scope for this slice.

## M2 — Backtester and baselines

Timestamp semantics, portfolio accounting, deterministic orders and fills, conservative versioned costs, ledgers, metrics, benchmarks, and simple baseline strategies. The current slice covers explicit next-bar fills, cash accounting, bps slippage and commission, decision/order/trade/equity ledgers, cash, buy-and-hold, fixed-weight, moving-average trend, time-series momentum, moving-average mean-reversion, and volatility-targeted exposure baselines, cash-relative report comparisons, deterministic fixture reports, and a separate complete-session boundary for atomic multi-symbol portfolio targets. Gate: no lookahead; accounting and order invariants pass; fixed transactions cannot benefit from higher costs; reports reproduce. Walk-forward evaluation and qualification remain M3.

## M3 — Experiments and qualification

Durable campaigns and experiments, restart recovery, split controls, walk-forward and robustness checks, comparisons, gate-based qualification, and protected holdout workflow. The current slice covers search budgets, pending/running/completed/failed lifecycle, heartbeats and stale-run recovery, explicit split classification, hidden holdout metrics with logged access events, approved disqualifying gates, range-limited registry-backed runs on cataloged datasets, chronological walk-forward candidates, cost and delayed-fill sensitivity candidates, bounded run and comparison CLI commands, score-free comparison reports, fingerprinted training plans that preregister exact candidates before execution, and controlled-run provenance with fingerprinted report artifacts for qualification sources. Rapid-002 adds one candidate-specific validation-plan schema that atomically reserves 28 exact records and derives every run input from stored state. Its 28 records completed, but controlled qualification rejected the candidate for zero fixed-weight benchmark wins and excess instrument profit concentration; no independent range opened. Provider campaigns retain superseded methods and corrected reruns. Report schema v2 adds risk-adjusted return, exposure, session and instrument concentration, and SPY regime metrics. Recorded training campaigns include rejected long-horizon per-symbol momentum and rejected monthly relative-strength portfolio families with predeclared parameter neighborhoods. The approved machine-readable policy records every gate and rationale. A registry-backed evidence manifest reproduces aggregate metrics and formal rejections in an immutable report without loading bars. One-time stored authorization replaces the raw holdout-creation flag and binds an exact qualified candidate to one post-validation holdout record. The controlled runner consumes that authority before reading data, scans only the authorized Parquet timestamp range, retains failures, and keeps completed metrics behind the separate read event. Strategic allocation passed all approved validation and one-time holdout gates. Gate: failures remain evidence; ordinary research cannot read holdout results; no single score can hide a disqualifying failure.

## M4 — Alpaca paper execution

Paper-only adapter, intents, independent risk, order management, idempotency, broker events, reconciliation, operational journal, and emergency disable. The reviewed design requires one transactional execution database, deterministic client order IDs, exact paper-endpoint selection, fresh broker and market snapshots, forward-only state, reconciliation before retry, persistent emergency disable, and append-only hash-chain evidence. Broker-free intents, journal verification, risk gates with no default values, default-on emergency disable, code-bound paper authorization, durable fail-closed decisions, normalized snapshots, explicit flat baselines, durable reconciliation evidence, fixed-origin GET-only paper account state, journaled adapter provenance, stable emergency-clear readiness, idempotent journaled emergency transitions, authorization-bound strategy-capital baselines, immutable fill-and-bid-marked strategy-equity checkpoints, complete attested risk context, transaction-bound attested risk decisions, atomic settled positive-fill capacity release, transaction-bound quantity-order submission preflight, one-shot fake submission outcomes, strict paper order POST normalization, durable one-shot cancellation attempts with unknown-outcome evidence, strict single-order DELETE normalization, immutable deterministic cancel-all order-set planning, restart-safe per-order plan consumption, immutable positive exact-lookup provenance, read-only terminal cancellation recovery, a fail-closed paper-write readiness runbook, dormant activation, opt-in, assessment, and revocation controls, exact activation-bound submission and cancellation attempt caps, a main-only attested-wheel build path, fixed-authority wheel and manifest verification, exact non-editable installed-distribution binding, transaction-bound runtime identity evidence, a fixed-origin production mutation transport, and an activation-bound production coordinator now exist. Exact paper process opt-in opens only the outer runtime gate; transaction-bound controls remain authoritative. Gate: unknown or stale state blocks writes; duplicate intents are harmless; reconciliation discrepancies stop execution; live endpoints cannot be selected.

M4 also includes a bounded unchanged-terminal replay recovery proof and explicit, idempotent local
schema initialization. The first real Alpaca paper session ran on 2026-08-04 and filled four
production-bound orders: GLD 3, IWM 8, QQQ 3, and SPY 4. Its initial-entry replay/shadow/paper
comparison passed. Present-state planning and an append-only non-flat authorization handoff now
support a later strategic-allocation session without resetting positions, fill economics, equity
peak, drawdown, or the root strategy epoch. Planning derives the inclusive XNYS session count from
the root fill-backed and current attested NYSE clocks and derives its market-state fingerprint from
immutable current evidence; neither value is an operator input. Declaration, completion, and
planning add no broker authority. The current Alpaca HTTP 401 remains an operator credential
blocker, and authority-grade verification still requires the unprivileged deployment account.

## M5 — Equivalence and sustained paper operation

Replay/shadow/paper comparison, supervisor recovery, disconnect and stale-data handling, and sustained paper campaign. The current slices add immutable broker-read-only campaigns bound to production-attested baseline positions, explicit healthy, drift, read-failure, stale, historical-continuity, and final-campaign states, CLI sampling, immutable action-plan comparisons across replay, shadow, and stored paper intents, and boot-enabled bounded systemd supervision of the same observation loop. Neither path has broker-write authority. Week 1 completed healthy with no drift and one recovered read failure, but its approximately 1030.755-second maximum gap, reported as 1031 seconds, exceeded the fixed 900-second limit during two VPS reboots. The formal operating-limit gate did not pass; the reboot cause explains but does not erase the failure. A later shorter reboot/recovery validation passed, and its longer follow-up remained healthy until it was stopped. The remaining 168-hour duration was explicitly waived; the unobserved period remains an evidence gap. Gate for any future sustained-paper claim: discrepancies are explained, recovery drills pass, and operating limits hold for the full period required by that decision.

## M6 — Bounded automated research

Rapid Research provides bounded deterministic sweeps, chronological walk-forward folds, execution stress, isolated result storage, and zero-authority export. A spawned-process executor supports bounded same-stage work with four workers by default and the unchanged `research-attempts-v1` retry boundary. Exposed 005 completed 272 runs, recovered 12 lease-expired attempts, and froze empty; its deterministic fixture benchmark remains the valid throughput measure at 3.426x. Event Drift, Event Repricing, Event Opening Breakout, and Event Prior-Low Rejection later closed empty under their frozen discovery gates.

Program 002 implementation is complete for `multi-hour-sector-etf-research-001`: twelve traded ETFs plus SPY context, two fixed cross-sectional families, eight configurations, one 11:30 New York decision, 232 maximum specifications, and 696 maximum attempts. The exact 20,280-row context dataset remains immutable and Controlled A/B remain unacquired. Authority v6 attempt `program-002-exposed-acquisition-20260826-v5` reused that context, created its volume projection, and froze seven Exposed Block 1 monthly segments through January 2021. Complete February pagination then returned 19,259 of 19,266 required normalized rows. Seven MDY coordinates were absent, including one consecutive pair, so both the foreign-coordinate and consecutive-gap stop conditions apply. Corrected incident v3 SHA-256/fingerprint is `ebc8f536d51fd701f9fc26f28630abff9f23a0b5edb3e794a5a3016337273618` / `1f5350eb3f704c343044cd11ac328643fd1ae0939ebeaa030248ce031938b53a`; finding-free review v2 SHA-256/fingerprint is `18ee95730f5af8698ff090c74df42774e1294dea8ff5552ad6c3b16495566e53` / `23c20fb78b71c3e527eb5c4f916d58463fdb311daf2a66436c5d32f976047cbf`.

Authority v6 remains revoked at source commit `0ee88843ff56a1a580475e4608e3e82b66123841`. The later one-use minute-source authority ran exactly once from clean main `589394e01142879c00112e4cf84d7783f492bd30`. Four exact Alpaca SIP `1Min` requests returned 1,168 rows in four HTTP 200 terminal pages. Source-proof reload exposed a `Decimal`-to-string representation mismatch, but independent structural aggregation found 305 exact frozen-control matches and exactly seven empty buckets: the seven authorized targets. This source cannot reconstruct a target, so no processing repair or retry is permitted. Failure SHA-256/fingerprint is `f95850c120603608f616f2e88d579424f4dc80301aa1251b7188fcca89fc08cf` / `b15a5acbce63758fe40619db1861f6e624d454651d9977fcddbda269b92cca2e`. Revocation commit `49bb011b69327b97fbee74e0862a60c0d58600ec` makes every data home exit 64 before comparator loading, claims, credentials, client construction, or transport; finding-free closeout-review SHA-256/fingerprint is `90f9f0a64a73951001846b305ff0927a249369224c8e7d03e4c2c30cfd599ae7` / `e887f347b37c8ecc01506e876ec893e0eae56442f5f2fd250328323f145d8707`. No source proof, canonical admission, exposed dataset, quote evidence, cost model, Campaign 1 binding, or real strategy result exists. Strategy execution, controlled/protected access, qualification, PAPER, broker writes, and live execution remain false. The next gate is separate user authorization for a prospective plan that selects a different source or an explicit independently reviewed missing-data disposition.

The prospective missing-data disposition now freezes whole-session exclusion, 100% coverage of all twelve ranked ETFs plus SPY, 57-of-60 coverage in every preselected quote window, one-session and one-percent fixed-period loss ceilings, one incomplete or same-symbol-incomplete session per rolling twenty sessions, and zero required-context loss. Its public boundary binds the exact reviewed 1,531-session exposed table and 657-window quote grid and exposes no schedule or policy override. Finding-free independent-review SHA-256/fingerprint is `7b23457dced43c78e8925ac5dad9d75da77feae7018ce91551c26a86d9df1372` / `d07ddce0ccb87770e458fafb2ff08f7d69a95f32ae70ea8f0cd91d251103bdd0`. The known MDY-only February gaps fail the disposition, so the attempted source remains inadmissible and a separately authorized different-source planning phase is the next scientifically acceptable path. No acquisition or execution authority is added.

The Massive qualification boundary is now implemented and mock-validated for the exact 630-chain sample, but all three mandatory pre-transport gates fail. Exact adjustment and provider aggregate-eligibility semantics are unproved, and public terms do not grant the required retention and derived-research rights. No credential, authority, provider request, real qualification evidence, dataset admission, or strategy result exists. Program 002 stays stopped. The next gate is executed written contractual evidence plus provider-authored semantic bytes, followed by a new independent gate review; full acquisition remains outside this phase.

Program 003, `multi-hour-sector-etf-research-002`, is proposed as a prospective low-cost successor without reopening Program 002. It binds the exact predecessor plan and keeps the same untested eight strategy configurations, chronology, campaign gates, and protected controlled-evaluation sequence while replacing historical NBBO with a universal 6/12/25-basis-point-per-side envelope and 5/10/15-minute delays. The sole source candidate is Tiingo's Beta consolidated historical intraday endpoint; raw unadjusted bars remain source evidence, split normalization is repository-owned, and missing data excludes the whole thirteen-symbol session under return-blind global and concentration limits. The fixed source qualification allows 221 GET chains and responses but has no authority. Current Tiingo terms prohibit durable Starter persistence and require a continuing $30 Power subscription while data is retained; required bar and corporate-action semantics also remain unresolved. The exact plan and finding-free review fingerprints are `f5b184ff3e1604a151a82214d1cf91fbdffa6fc4fddf7d7ce0506a2e99427a42` and `55ea3dcb3fc122034b59909bd8431886aa61fac5e8c83ea2a53eb9fdbd060bdb`. The next gate is a user decision on licensed retention plus provider-authored semantics, followed by implementation, synthetic verification, independent review, and a separate one-use qualification authority. No market-data request or strategy execution is authorized.

Program 004, `multi-hour-sector-etf-research-003`, prospectively replaces only Program 003's Tiingo data and provenance architecture. MarketParquet's current standard license supports the needed private perpetual retention and one-time archive model; native five-minute, split-adjusted, non-dividend-adjusted ETF files are mathematically compatible with the inherited same-session strategy under fractional-share and bps-on-notional accounting. The exact plan and finding-free review fingerprints are `2b740810f1365d20c11d1cf3bb83cdd0e06e72c30a5070ff0648031f29d893fc` and `5343b70c5cfe9e6c4342286cb6e1bac9c75b8344b620ce1110d69f90ec99efd0`. The later full range contains 1,531 XNYS date files: twenty context and 1,511 exposed evaluation sessions, with at most 1,516 additional downloads after qualification reuse. The next possible gate after green CI, merge, and a new user grant is only a capped manual Complete Archive purchase and fifteen-file structural qualification. Full exposed acquisition and every strategy stage remain separately unauthorized.

Program 005, `multi-hour-sector-etf-research-004`, abandons the paid Program 004 path before purchase and prospectively selects free Alpaca Basic historical SIP. Official pages retrieved August 27, 2026 confirm the zero-dollar plan, history since 2016, SIP access for an explicit end at least fifteen minutes old, native five-minute bars, inclusive bounds, token pagination, adjustments, and 200 requests per minute. Raw and paired `split,spin-off` views preserve source evidence, raw fills, and cross-session price/volume comparability. The exact `7/1499` global loss ceiling comes from Program 003's independently reviewed source-neutral Tiingo plan, which expressly recorded that the known MDY pattern failed its old concentration controls. Five MDY dates form one immutable pre-exposed design quarantine and leave two isolated unexpected slots. Its hash-bound complete Program 002 incident inventory, discovery-block, month, year, five-minute-clock, strategy-clock, nonadjacency, and six SPY/MDY morning-tail gates are immutable; the class cannot be subsetted, expanded, or reused, while inherited recurrence controls govern every future unexpected exclusion. The one-use qualification freezes thirteen inclusive ranges, twenty-six paired chain identities, per-chain two/six-page caps, 28 expected and 60 maximum responses, 64 MiB, and reuse without resegmentation. Its sample contains no realized spin-off, so every later in-range spin-off remains admission-blocked until issuer/exchange evidence and exact paired-factor validation pass. Hash-bound issuer pages record current 0-2-bps 30-day median full spreads for all traded ETFs. Corrected plan SHA-256/fingerprint is `3a71573086418aa8ff53d8359110dee1a951caa333ffd35eccedd8d38678cb11` / `79a73d143700c643d67c2f862b5bfe3655df9706276a1b70e189c425d4397cb7`; public-contract evidence fingerprint is `bb389757b60777cc20549c58201b71f130151dfb1ec65d2959ec1b082f911c2e`; finding-free independent-review SHA-256/fingerprint is `6155632a474351084d7a8b6670dde0ddf30f7cca3d6bd77abad9f4d5546c493e` / `276e08b440012739d36d666c05cec2ba6421f4e381c6b148752fb2c7682960e6`. All fourteen required challenges are addressed, thirteen focused tests pass, and challenge 11's material licensing ambiguity remains an authority failure. The customer, Nasdaq, and NYSE agreements do not expressly grant private immutable retention, so the next gate is written contractual resolution without a paid plan, followed by a reviewed provider adapter and separate one-use structural qualification authority. Executable mocks add no request capability. No provider contact, credential, request, acquisition, strategy return, or protected authority exists.

Program 005 implementation update, August 28, 2026: the user's private-retention direction resolves the planning gate for private personal research while public redistribution stays prohibited. The public recipe now includes the exact adapter, private create-only page and dataset storage, deterministic paired views, action and missingness admission, fixed-quarantine recomputation, credential isolation, repository leakage checks, and a credential-free CLI preflight. The implementation-bound proposal is non-active with every authority flag false, and its fresh closure review is finding-free. Qualification execution still requires clean synchronized main and separate user authorization for that exact one-use scope. Full execution remains blocked pending exact qualification-byte, receipt, and review bindings. No credential was loaded, no provider request was made, no observation or strategy return was generated, and every protected, PAPER, broker-write, and live flag remains false.

Program 005 activation update, August 28, 2026: the separately authorized attempt stopped before credentials or authority consumption. Fresh review rejected a candidate whose source changed one proposal-bound file, then independent validation showed that the active-authority loader accepts re-fingerprinted changes to claimed user-authorization and review provenance. The request plan and one-use runtime boundary remain bounded, but the exact authorization contract is not enforced. The candidate was discarded. Review SHA-256/fingerprint is `f7df27d39c8bbef5134b7238981571e660194de26486fbd7a70d177636743df8` / `76314643d8dd1b6f88620c8340a58bbd1847c70dcc377836671cd0feac9f7c11`. No active authority, private root, claim, credential load, provider request, observation, or strategy return exists; all protected authorities remain false. Gate: repair the loader, issue and independently review a new proposal, merge through green CI, and obtain new exact user authorization. The current authorization packet cannot transfer to changed bytes.

Program 005 authority-binding repair update, August 28, 2026: authority v2 is mechanically derived from exact proposal, implementation review, proposal review, scientific scope, authority flags, and a ten-file implementation root. Runtime requires the external user-authorized root, fixed ignored authority path, clean synchronized main, immutable control-artifact lineage, unchanged reviewed source bytes, and a second full validation under the one-use lock. Implementation commit/root is `928e6bb797be987d5e37c180a23fdd5f2476b603` / `82659ef32ddc00968bd588e24de1677d51788de23edca321cab93f37750f7114`; finding-free implementation-review SHA-256/fingerprint is `041d7926652663a0382ea960d7bb4b3085febb83d6d17f4ae3d6822928ba79b0` / `60b2eaa62d84e36b716fdd3b52943cfa60c4f27fff97d0686ff83f477e6a93a8`. Stage A merged as `3bdc2087f3a0e965d6d64fd24e0de22a98ba27b0`. The inactive v2 proposal `program-005-source-qualification-authority-proposal-2026-08-28-v2` binds that merge with SHA-256/fingerprint `c3ebe6c5ca36cba26468fe75af952b0f8019188660840b287e7503e945335b00` / `e74b8131cf82646b56b2609e2c9de077cebe25993a2dcec7bcac6bb4735b2e6b`; its fresh finding-free review SHA-256/fingerprint is `ff17381bac27ec3e049650fdfd5471e6f13c3c925ec474b01ff259fa0e8f6c53` / `f72387b3960223945de3ec37d5e93ed61ec3da1c01e45a0f9f01d5bd7f682923`. Credential-free preflight passed 22 sessions, 13 symbols, 13 ranges, 26 chains, 28 expected and 60 maximum responses, 64 MiB, 120 requests per minute, one future credential load, and zero retries. Stage B must pass green CI and merge on clean synchronized main before the exact root can be reported for separate user authorization. No active authority, credential access, provider request, qualification, private data, strategy return, or protected action exists.

Program 005 terminal update, August 28, 2026: exact root `a5f95e11a5e636d88bbaeecb149b290899685d297cd797b49c6b23ed2f5f5620` activated authority v2 on clean synchronized main `ed851e526bd2dbd5dee56f092739e6192eb29864`. The one authorized run created its claim, then failed before client construction because the required credential pair was unavailable. Successful credential loads, provider requests, responses, bytes, and retries were zero. The consumed result is `FAIL`; no structural source property was observed. Failure SHA-256/fingerprint is `20ff82cf99ddd396c3b1dff73df2dfdc4a002278270d631f6de1ca1b8deed99f` / `51bd01b08b99746fb2379cd3afbb6a51609f0d9dffcb643f36e73e3a6cff4841`. Finding-free terminal-review SHA-256/fingerprint is `00400b86622bf0258f59a818260a8f4bfff847413061db1509b6b9dec75c0cd2` / `23fe61c933cece1a81a2cca5ecc6490dc6aa7d98f9a09946e8560e0ba38435f2`. The closeout changes an authority-bound file, making v2 invalid on later clean main. Program 005 stops: no retry, v3 authority, full acquisition, dataset admission, fallback source, or strategy execution.

Program 006 authorization-readiness update, August 28, 2026: `multi-hour-sector-etf-research-005` remains scientifically identical to Program 005 and now has a reviewed v2 READY packet after a names-only credential preflight passed. Implementation commit/root is `d747a4c6f4de67cbaab063a812f757345a9b0aca` / `6d0c00841f664ad26dabd846b096e4e2c02450a877f97503289d9994e652e089`; implementation-review SHA-256/fingerprint is `0eb3af2419ff1ffa0ca4155f6c9dfa4a18cbe7f26a70d690413cd9bab3d65e61` / `53505ed9b86d6be3779509064971cb7d33dc1c1dddd0300fafa91726933d2fbe`. Proposal v2 SHA-256/fingerprint is `4df295b78a76afe4ef703ec30446dcfb12ba2f0e152c7ab476c885a53e631003` / `b52d34c4666039bdd563515cce727eb05b4a9757ee68c49e4af5509641147793`; review v2 SHA-256/fingerprint is `467a862378b80b81c0ea337d6eda531e5f4b5af0b6967b8cae5efe222386be9e` / `bee7583e83c7dbbd6cf7bc0efc9a7361d9d3734f1217b24aeafa99afa3a85673`. The external root binds the exact final synchronized main commit and all control add commits, so it is derived only after merge. This readiness state was consumed by the terminal run below.

Program 006 terminal update, August 28, 2026: exact root `56125cd74d917c938b1076160f3a6e7c408149d4048d8b0f55f693262fec47c2` activated on clean synchronized main `8ed3bc3f700bbf2b014527c12dc45e0e8cb26def`. The one-use run loaded credentials once and consumed the authority before its first transport. Request and response 23 failed the exact XNYS timestamp grid on the first raw pagination-split page after 2,503,402 total bytes and zero retries. Offline checks on 22 stored pages found 13/13 symbol coverage, the same nine fixed MDY gaps in both views, no unexpected gaps, and non-constant paired adjustment price factors in all 11 completed pairs. No receipt, source manifest, dataset, or strategy return exists. Failure SHA-256/fingerprint is `3c693b2a31c5d5eef11388b55e41f38b9203c21e7f8ce604fc8f89c9c15e9687` / `acbbbf0f28cc5d555224e05fb8d34cf4e9ae1a60fe75020ca2fee03da91180e5`. Finding-free independent-review SHA-256/fingerprint is `7e79c4534ad2df67c99d70de013033f823ad650d8e35c3bdf9a8d782bba0adf9` / `59533d3b6c151865d304ed11aaf1a9917050625ed736b7d5edb1b38995b35359`; it binds closeout commit `d3e7f10d33d5926074b59913143b7190b0f1bf75` and the explicit terminal guard. Program 006 stops without replay, replacement authority, source switch, full acquisition, dataset admission, or strategy execution.

Program 006 forensic update, August 28, 2026: response 23's exact row remains indeterminate, but the old every-row-on-RTH-grid rule is a qualification-specification defect. The paired adjustment rule is also defective because it assumes exact combined price scaling and reciprocal volume from one row-specific factor despite documented split price-and-volume and spin-off price-only semantics. Retained volume matches the public split ledger in all 10,677 rows; provider adjusted-price behavior and source suitability remain indeterminate. Program 007, `multi-hour-sector-etf-research-006`, is proposed with raw SIP pages, raw same-session prices, deterministic RTH projection, and exact split normalization only for relative-volume share counts. Its 15 sessions are selected from 1,202 sessions left after excluding the hash-bound 185-session Programs 002-006 observation union and 145-session in-chronology protected inventory with one overlapping session; its six chains allow seven to 11 responses, 16 MiB, one load, and zero retries. The proposal binds the unchanged Program 005 whole-session missing-data policy and makes qualification stricter by permitting zero incomplete selected sessions. The forensic independent review passed all eleven required challenges with no findings; its SHA-256/fingerprint is `103c58c47b0c07c768bd5b3efc577704a74f83cdd223f9945b2df00a3b6f099a` / `0d389c0209753a64dbe0362dba9b817f33103e3a0b7c7ed89164e2c2747d32fe`. The next gate is a complete issuer/exchange unit-changing action ledger, raw-first implementation, finding-free implementation review, clean synchronized main, and separate exact one-use authority proposal and user grant. The current proposal grants no credential, request, qualification, dataset, strategy, controlled, protected, PAPER, broker, or live authority.

Program 007 coverage correction, August 29, 2026: the offline-only raw-first module, exact XNYS projection, completeness gate, pagination controls, restart boundary, and nonreconstructable public summary remain implemented, but ledger v1's eight no-action claims are superseded. Ledger v2 resolves the ten Select Sector SPDR symbols, records five 2-for-1 splits effective `2025-12-05`, and leaves IWM, MDY, and SPY `COVERAGE-UNRESOLVED`. The public NYSE `corpax` archive covers 319 date intervals and 11,345 unique dated events but excludes forward splits; the date-bounded issuer/SEC screens do not close that gap for those three funds. Any unresolved symbol blocks the whole Program 007 dataset, ledger-backed normalization, and one-use authority proposal. V4 and V5 remain immutable failed-review history. V6 holds all synthetic evidence in one unnamed temporary-file capability, uses the visible workspace only as a root-identity and foreign-entry canary, and serializes canonical create-only evidence entries with a per-source `RLock` plus `flock`; restart works only through the same source object while its descriptor remains open. A future real transport still requires a separate authority-gated persistent store. Ledger v2 SHA-256/fingerprint is `3b815581d0da66db427243bce34f9ced5021f73719acd2b5d5e277d57065d53a` / `0ec39d6f38d469e099862173ff710c0e737b39b464e233e291c9e9b20c089c25`; implementation v6 commit/root is `b06b6c77b170705ce52b18ae5058e96f861c29e1` / `1e3fc58344fa2c4f72cd8f3638c63f9fd2e9e69ceb8c7265a2b7768aed7c9df8`; artifact SHA-256/fingerprint is `9903d4c243e94c34879cc5edd086747b4687b33224167db549782229f259b188` / `4e611bd85f4f59f045c1cf981ac77cc5359fce7205bedf698bd3f84050c57456`; finding-free independent-review SHA-256/fingerprint is `e0cb67fed583490fbea484f7067a8ffcb93f663381a6cc51b80c0db3522d8238` / `7c6517671aa9a8e0fa9f13e3f1adf94343639c45b49a602b4efd8cc62b3b6841`. The unchanged sample passes its 14,742-coordinate synthetic shape in seven responses; 46 focused tests and the sanitized full suite pass, with 1,335 passed and four skipped. Gate: complete public issuer or exchange unit-action coverage for IWM, MDY, and SPY in a reviewed successor ledger. This slice adds no provider client, credential path, source authority, acquisition, dataset, strategy calculation, controlled/protected access, PAPER, broker, or live action.

Program 007 metadata update, August 29, 2026: a separate standard-library synthetic adapter and prospective plan now freeze `GET /v1/corporate-actions` on `data.alpaca.markets`, all thirteen symbols and CUSIPs as two reconciled chains, all sixteen REST event arrays, `region=us`, `data_quality=complete`, a conservative `1990-01-01` start, four pages per chain, eight requests/responses, 1 MiB per page, 8 MiB total, one future credential load, zero retries, and a terminal no-purchase/no-fallback HTTP 403 stop. Stable-identity forward and reverse splits alone receive exact factors; every undocumented or non-simple transform fails closed. The five public 2-for-1 actions are mandatory positive controls, and the five public no-action symbols are investigation controls rather than reasons to discard new metadata. The current REST contract does not prove predecessor-identity closure, date-filter meaning, bounded processing lag, or Basic access. Identity history and source finality therefore block authority, empty results remain provisional, ledger v2 stays 10/13, and no ledger v3 exists. Plan SHA-256/fingerprint is `78a85b3f1057096bb49d5d4c83c60e3353c6a0c695f17f6db22880223d5568b4` / `f5925f99991913f262d50f3946836ac130acbf347bc4cbe490bc950a1f5d2d3f`; implementation commit/root is `baf2689e62f6e778c11822232ebc6fa0e07bc652` / `c508486d29788f562b63b21b3edd9e5dc871bd5fd82274afe9b9927774d13be9`; implementation artifact SHA-256/fingerprint is `5480c274027088303cb14f6ea2a8c93d7493d290eec76dde25bbc91fd4d73f48` / `6bf04f9078a9e24faf037cde51147fc9b046c96ca33dabe184d9f5041022337d`; finding-free review SHA-256/fingerprint is `078ff4710e2b101b355812ab5907c5b498dc625e6c9fe5d753ce5224ae7c7a6f` / `b2ef7fbb017fe5870406e7c478e7a400995653af4472a80568abb657939668be`. The next gate is reviewed predecessor closure plus provider-documented date-filter and maximum-lag semantics, elapsed lag, a newly frozen end, and separate real-transport/persistent-store review before any metadata-only user grant. The 14,742-coordinate OHLCV sample remains unchanged and unaccessed; no provider, credential-value, metadata, OHLCV, strategy, controlled/protected, PAPER, broker, or live action occurred.

Program 007 metadata repair, August 29, 2026: plan v2 replaces the impossible bounded-creation-lag gate with bounded as-of corroboration against complete public ledger v3. The future query still requires both all-symbol and all-CUSIP chains, five positive controls, discrepancy failure, all sixteen current REST arrays, `region=us`, `data_quality=complete`, `1990-01-01` through `2026-08-29`, four pages per chain, eight requests and responses, 1 MiB per accepted page, 8 MiB total, one future credential load, zero retries, no purchase, and no fallback. The module now includes the fixed-host GET-only bounded transport and create-only persistent store, but only an explicit mock transport can reach the persistent executor. Plan SHA-256/fingerprint is `794ebe3be9e2483980043b498f52772f590f4e6b1629217a6c8a2cd5bf7db93a` / `add9b3bf1cfe3b81cae4d1e856fb99b6e27bf16133173e76e725303a9e054ead`. Gate: freeze the implementation binding, obtain a finding-free independent review, pass all repository gates and CI, and merge to clean synchronized `main`. Only then may a separate reviewed one-use metadata authority proposal be requested. This change creates no authority, provider request, credential check or load, metadata observation, OHLCV access, dataset, strategy result, protected action, PAPER action, broker write, or live action.

Program 007 metadata closure, August 29, 2026: immutable provider evidence and plan v3 bind Alpaca's no-creation-time guarantee, inclusive interval wording, `process_date` sorting, and all sixteen current types, including `capital_gains_distribution`. Implementation v6 remains mock-only and source-disabled for real transport. Its source commit/root is `fbbe3bf1615799233bce3488949a2c9ac560ce7f` / `4905cc76efdae0281aea935c0dc49d47a4544763e3648de16c02446eeaae4260`; artifact SHA-256/fingerprint is `14ccfd69d54d9035cc36ccc7915c044c2596c06836e400da0016d5c74e9e84eb` / `0d87ce202517fdd1c41275c6d81f9d937189dbb81f33ac7735df3c428791a289`. The fresh finding-free review SHA-256/fingerprint is `2a1199e223d036723040420e4e75abb9cbdfc503a35a88277164f9660c20a80b` / `d3aac3e4774096311339814277ed575df6d1744ca032702c0e50c4e36222b2e5`. Gate: green CI and merge to clean synchronized `main`. A later one-use metadata qualification still needs its own reviewed proposal and explicit user grant. No authority, provider request, credential access, metadata or OHLCV observation, dataset admission, strategy return, protected action, PAPER action, broker write, or live action exists.

Program 007 metadata authority readiness, August 29, 2026: blocked proposal v1 and its review remain immutable. The names-only credential preflight passed, and proposal v2 is `READY FOR USER AUTHORIZATION` with a fresh finding-free review. It preserves request plan v1, ledger v3, plan v3, implementation v6, the five positive controls, the two-to-eight-request and response budget, one credential load, zero retries, and the first-transport consumption boundary. Gate: green CI, merge to clean synchronized `main`, report the exact external root, and obtain a separate exact user authorization before activation or execution. Every authority flag remains false, and no claim, provider request, provider byte, metadata or OHLCV observation, strategy result, protected action, PAPER action, broker write, or live action exists.

Program 007 metadata terminal update, August 29, 2026: exact root `a3e65f85bcc9adaedcfe3842d3b148ed1fb6230ac303ff2cdedadfe33c0bfbbd` activated on clean synchronized main `a624acbe86177323e88042c5dbfe6ef00862e1c8`. The consumed run loaded credentials once, made one zero-retry symbol-chain request, and received one HTTP 200 response of 115,628 bytes. Frozen schema validation failed on an empty required cash-dividend `cusip` before the CUSIP chain or reconciliation. The metadata path is terminal; its private response remains ignored. Current code rejects activation and direct execution before credentials or private state. The finding-free terminal review binds closeout commit `e3de52b3b375d1128bfe27453ae9be48420092fc`. Program 007 OHLCV qualification, dataset admission, and strategy execution remain prohibited.

Program 007 metadata forensic update, August 29, 2026: current first-party documentation and the retained response prove that the universal non-empty-CUSIP gate was a qualification-specification defect. The 543-event page contains 538 cash dividends and five forward splits; 189 dividends have empty CUSIP, all 543 rows have empty ISIN, and all five public split controls are present and consistent. Program 007 remains terminal. Program 008, `multi-hour-sector-etf-research-007`, is proposed with only the never-executed thirteen-CUSIP chain: one to four requests and responses, 1 MiB per page, 4 MiB total, one credential load, zero retries, and no replayed symbol query. The finding-free independent review is bound at SHA-256/fingerprint `0ff5fe1f2c3db02c468ce3f87b553acbdd94b290189eb9f03c843e071c18475d` / `e987474a6af543a0e7097db67ee87475d0042b2ceae7b3c7e1b49d07e67b4064`. Gate: green CI and clean merge. Any execution still requires a separate reviewed one-use authority and explicit user grant. No active authority or claim exists; the forensic phase generated no external root.

Program 008 metadata authority readiness, August 30, 2026: the names-only credential preflight passed and the exact one-use authority proposal is `READY FOR USER AUTHORIZATION`. Implementation commit/root `f0ed2534e768d26cc5969d574d16f0ef951e28ac` / `186168ba7fd738e2fa54e9a785808df726a97928a32a9f3e6969f8ff77adc507` removes arbitrary production transport injection and fixes execution to the redirect-disabled corporate-actions endpoint. The request plan remains byte-identical at SHA-256/fingerprint `882682b16bbf9f5fa81742a75577546f364d7c76bb04e6e15abf2d9a64043465` / `18ae8ef4152f827fe70261c76f20baf026b44e1a091ae98f1537dcacda818fe7`. Authority-proposal SHA-256/fingerprint is `2f7ca46c70fb1aeef298728c780fca28c16c9121bedba1ec1ea5d99954268da5` / `6173cb00ab3119ad17846c0251d253c4448e017e344b882b104ef966c7bc3f78`; the fresh finding-free review is `c80decf235f7447740b895f400c3faa9386f8f5db10322bdfeb64a66219a7f66` / `ec2b111145b20d59f92dd7b3cdbab8a04572d1889db388732690709a3d73f66f`. Gate: full quality checks, green CI, clean synchronized-main merge, and one external-root derivation. Activation, claim creation, and execution require a later exact user grant and remain prohibited in this phase.

Program 008 metadata terminal update, August 30, 2026: exact root `90b60b95caef446d84e8242ae9aa3e0086d2655c7f55da9dc28c7d252153ad9e` activated on clean synchronized main `37af39a2213ac8bb29b2877d452bf480a33b096a`. The consumed run loaded credentials once and made one zero-retry, redirect-disabled CUSIP request. One HTTP 200 page contained 83,338 bytes and 354 unique events: 349 cash dividends and five forward splits. All records mapped to one ledger identity, all five `2025-12-05` 2-for-1 controls passed, no relevant discrepancy appeared, and pagination ended with a null token. Terminal-success SHA-256/fingerprint is `23bf0e29b4f8b4b4655d7eeb470e4ceb1bc319717c854ec3d82790ef52c1762b` / `151091ac4d863d73561afc24dc0138e5326dd237183dfb7da178cb5584871fcd`; finding-free terminal-review SHA-256/fingerprint is `65eda8a7e6bc6d262b382227db7ca82ecd42dc38caa97d637d6dd1b207a4e6c9` / `143f8e5023b96bc950f9ec86d37e966fca42ceefd1c07c691d6d3c2af7c66dad`. Program 008 metadata is terminal and cannot replay. OHLCV and every downstream or protected phase still require a new reviewed authority proposal and explicit user authorization.

Program 009 OHLCV terminal update, August 30, 2026: `multi-hour-sector-etf-research-008` is `TERMINAL-FAIL-CONSUMED-NO-RETRY`. Exact root `a21491c95307ec9a2d86837c7827fccf6d32d165a069e97cf92ec050247e98c6` activated on clean synchronized main `56278b8f2dc8714f58b026b0a13d523072bb64ba`. Nine zero-retry HTTP 200 responses totaled 1,806,300 bytes. The three normal chains passed, but the sixth allowed pagination page still had a continuation token. Execution stopped before page seven and the two remaining chains. The private manifest binds 17,281 raw rows, 4,344 valid raw-only extended-hours rows, and 12,937 retained canonical coordinates against 14,742 required. Finding-free review SHA-256/fingerprint is `2f96ed2d40740a1f24b9ba14583478fa99cc6700735b0cecf7fbe7ec3d3cf59c` / `6aaa8f1f0a210ae4c49449030800b5e5735ba6dade388f41fecf5304d5ef1bac`; 60 focused and 1,470 full-suite tests passed with four skips. Program 009 cannot replay. Full acquisition, dataset admission, strategy calculations, controlled/protected access, PAPER, broker writes, and live execution remain prohibited.

Program 010 design update, August 30, 2026: offline forensics classify Program 009's six-page ceiling as a qualification-specification defect, confirm one completed-domain MDY source gap, and separate 244 unobserved XLY coordinates from missing source data. Proposed Program 010 uses exact single-session multi-symbol raw SIP chains, `limit=1000`, token-driven completion, a 16-page operational cap, and explicit `SOURCE-MISSING` versus `UNOBSERVED-BECAUSE-CHAIN-INCOMPLETE` states. Its deterministic five-session fresh sample has 4,602 coordinates. Review required source closure, strict-majority per-symbol coverage, public-artifact guard coverage, invalid-request closure, validation of received symbol-then-timestamp order before normalization, and direct proof that retained Program 009 pages follow that order. Implementation and proposal v5 remediate all findings and preserve v1-v4 as history. Finding-free independent-review SHA-256/fingerprint is `41062ea7286a339651818025886dbbf049f2372d7cefc18431322c442e85bfa9` / `5b39c5b6107f3d63292da184a55b32d837e6f513c42d2253a3b38fbda70e4b04`. Gate: full quality checks, green CI, and clean synchronized-main merge. Any real qualification still needs a separate reviewed one-use child authority under the standing mandate, with fixed runtime controls and no external user root.

Program 010 terminal update, August 30, 2026: the reviewed child authority activated on clean synchronized main `e663235e35f2495d72de3dd709842e484dcf555e` and consumed its one use. One zero-retry request retained a 105,852-byte HTTP 200 response before frozen received-order validation failed. All 1,000 retained coordinates were unique and expected, every per-symbol array was timestamp-ascending, and only unordered JSON object-member encounter order violated the global check. Program 010 is `TERMINAL-FAIL-CONSUMED-NO-RETRY`, classified `QUALIFICATION-SPECIFICATION-DEFECT`, and cannot replay. Public terminal-failure SHA-256/fingerprint is `2e224ab7eb63bf8e844a436457705bdc3db2be34797570c70c254e0286d890c8` / `af0425dfdf4a84f88e278b69fac31d0233a71e8790bd51fd37b211487c1b95c6`; finding-free review SHA-256/fingerprint is `8a75496aeb22b8552d99141ab284b17daabb5f7f5cdf967aa60524e485d42597` / `943df3daeb0056c2acce0b6003722db66c25bc62156b46a050896fbdf71b80d7`. Gate: merge the immutable closeout, then create Program 011 with per-symbol timestamp-order validation, deterministic post-validation sorting, and a prospectively frozen sample excluding all observed OHLCV, including `2021-05-25`. No full acquisition, dataset admission, strategy, controlled/protected, PAPER, broker, live, or purchase authority exists.

Program 011 design update, August 31, 2026: `multi-hour-sector-etf-research-010` is `PROPOSED-NOT-AUTHORIZED`. It reuses Program 010's raw-first pagination engine and preserves token, repeated-page, duplicate, cross-page progress, missingness, strict-majority coverage, resource-cap, and zero-retry controls. It ignores JSON object-member order, validates strict timestamp order inside each symbol array, then sorts rows deterministically by symbol and timestamp. The fresh sample excludes the 199-session Programs 002-010 observed union and protected chronology; seed `program-011-raw-sip-qualification-sample-v1` selects `2021-04-28`, `2025-01-06`, and `2025-02-27`, plus untouched controls `2025-11-28` and `2025-12-15`, totaling 4,602 coordinates. V2 supersedes v1's incorrect literal bound-test count without changing the scientific or authority contract. Implementation SHA-256/fingerprint is `6bc00a76f5681cec5ae560ede1fd76a1ae8e02eb6954a0e483b3f129e8ddd05d` / `3a4a310fb9e4eb0baee3439f5a626710c57c1d5070e0546d33fc3b2904a97bb9`; proposal SHA-256/fingerprint is `ed25205a35bb89ce36326c8e554b92661d63b117015ca6762bb65860a4496766` / `ab2da3f3d08b9267c6f03b93e09ccd4cd20bbe8359aba2e6f36fbf507f307ae0`; finding-free review SHA-256/fingerprint is `f66d7aa735ef48005c911bc68a5c3fc45c9aa41b0015b1f7916d0b77b1490332` / `83eeda356a807b266fb93c3e477e7e73469c05f8dca74bf9901c7de075f12b92`. Gate: post-binding quality checks, green CI, and clean synchronized-main merge. Only then may a separate exact one-use standing child authority be created and reviewed. No credential check or access, provider request, dataset admission, strategy, controlled/protected, PAPER, broker, live, or purchase authority exists.

Program 011 authority implementation update, August 31, 2026: the fixed successor runtime reuses Program 010's reviewed one-use lifecycle with a separate ignored private root, fixed-host GET-only transport, disabled environment proxies and redirects, synchronized-main and protected-chronology revalidation, raw-first fsynced persistence, one credential load, an atomic claim immediately before transport, zero retries, and terminal evidence. It binds Program 010's immutable terminal failure and review, rederives the 199-session observed union, and exercises reversed JSON symbol-member order in the passing synthetic path. Child v1 is rejected before review because its execution and CLI preflight could inspect credential presence before validating the reviewed child, synchronized-main lineage, and chronology. The runtime now targets v2 and validates every public control before credential presence or private-state access. Gate: commit the repaired runtime source, add the exact v2 one-use child and independent review as its only successors, then merge through green CI before names-only credential preflight or provider contact.

Program 011 terminal update, August 31, 2026: `multi-hour-sector-etf-research-010` is `TERMINAL-PASS-CONSUMED-NO-REPLAY`. The reviewed child activated on clean synchronized main `f33fb5058de6176c0b32622d0e4eda70c64b4d74`. Nine zero-retry HTTP 200 responses totaled 490,879 bytes. All five frozen session chains terminated with null tokens, all 4,602 expected coordinates were present, and source-missing and unobserved counts were zero. Public terminal-success SHA-256/fingerprint is `cdb6aa8ee9c317738399d72a8a3af60882eaa959fb554a3765326fdf3cedfa21` / `8174a030b37fb153acda1b3deb519be32910be52341475b4adc29bfcee6c29aa`; finding-free independent terminal review SHA-256/fingerprint is `c2c019b94486a2d9cbe8cd53e1da6dbffadf36a265a57f4f47610a22b51929ac` / `86c5f4dae4ce96c4bae9bd4be4886d90c28d851c71831f5169244d112bd50620`. Program 011 cannot replay. Gate: merge the terminal review, then create a separate reviewed standing child for full exposed acquisition and structural dataset admission. No dataset, strategy, controlled/protected, PAPER, broker, live, or purchase authority exists.

Program 012 design update, August 31, 2026: `multi-hour-sector-etf-research-011` is `IMPLEMENTED-PROSPECTIVE-NOT-AUTHORIZED`. The active `2026-01-02..2026-07-31` protected range truncates the lawful exposed prefix at `2025-12-31`; its 145 sessions cannot enter missingness. The exact raw SIP request includes 20 context sessions plus 1,366 prefix sessions, for 1,386 session chains and 1,399,788 coordinates. The prefix has 1,354 full sessions, so the prospective ceiling is `floor(1354 * 7 / 1499) = 6`: five fixed quarantine sessions and at most one unexpected session. Program 012 binds raw prices and the Program 007 public action ledger, makes no Program 002 quote or admission claim, and grants no strategy authority. V3 supersedes unexecuted v1 and v2 after review found pre-send-intent, crash-safe credential accounting, exact quarantine-inventory, and inter-process locking defects. V3 requires the existing exclusive private-root lock, atomic fsynced request intents, value-free pre-access credential attempts with conservative accounting, and the exact nine-coordinate fingerprint. V3 SHA-256/fingerprint is `337a5b14ff15f9d40d0f88ed05822cf9e55293fe6c5219f56d63f1d65a67c19a` / `7f5817707001b03765ee5563fcb07f728ac066cc7352137b732f87312743c80b`; finding-free correctness and credential-boundary design review SHA-256/fingerprint is `3a61db10f5cd074ea3d3d1b446eaa4acd6b8bbdebe8b4c2dc13328ed58cf30e7` / `98736a47227b309447c34a8731edb2b7bff8c5e64a9392329145eb444cd5eb4d`. Runtime commit/tree/root is `da18b55f6e16234dc93fdb61801847a79e4fc178` / `709a0f86d2acbe2a9f2667c68acd210f13a3cf6d` / `a00fd6ceda78d6733b51af143d0d1ddfd87b6501e78ffab0245f0631dc347c70`; implementation fingerprint is `5fc2dc3789f3689307d4b7b352880f51c19753d498fab51d7ae18627152cb080`. The runtime provides atomic intents, parser-independent receipts, restart accounting and pacing, exclusive ownership, and structural admission. Focused and full local gates passed before this provenance checkpoint. No credential presence or value, private market data, provider endpoint, controlled/protected chronology, PAPER, broker, live, or purchase path was accessed. Gate: fresh independent runtime correctness and credential-boundary reviews, clean merge, then a separate reviewed one-use standing child before credential preflight or provider contact.

Program 012 runtime v2 update, September 1, 2026: finding-free correctness and security review closes forged recovery identity/gate state, generated-terminal re-entry, public low-entropy commitment, and durable credential-count defects before authority. Recovery rederives identities, gates, missingness counts, admitted-session bindings, and private manifests from canonical evidence. The public terminal omits detailed failure classes and private terminal, missingness, admission, dataset-manifest, claim, and private-identity commitments; proposal-required response-manifest and canonical-row hashes plus aggregate counts remain public. Implementation commit/tree/root is `f64a8f33631d0736ae447c7c74ee69f396d85bd1` / `a82f21c8fbd4c7b26564b7690db1263d98f77cf9` / `b4fab6feec31a4b536d015f6debb0dbdc18afcb440507375c55f7bf180049c46`; artifact SHA-256/fingerprint is `971d83c19dd4e1c81cb61792d02f3f659297cc5eb4fb0b248c19945cfa14c122` / `d805de1546f2512acbe1cac66269b1371c14ed8bdd8dbb663d00bd08b8da3ecc`. Twenty-nine focused tests and the 1,618-test full suite passed with four skips. Gate: superseded before authority or execution by proposal v4 and runtime v3.

Program 012 runtime v3 update, September 1, 2026: proposal v4 preserves the frozen chronology, admission, and disabled-authority contract while removing all public data-derived commitments and dynamic acquisition state. Runtime terminal publication and CLI output now share one fixed projection: authority/source lineage, result kind/status/admission, a pass-only public-control lineage identity, static assertions, and observation time. Private content identity remains bound to response, canonical, missingness, admission, and session evidence. An ordinary post-consumption exception becomes one generic sealed-failure message after private and public terminal persistence. Implementation commit/tree/root is `f6d5d18e30de20d3b96aaaf567b4529a25974297` / `1e0bb08a876dd661e5ec74564b1ac1cd5f3caf92` / `6d248959a24feae817987bd3fc0a7a570f9614351570599bd804a4649e4801ac`; artifact SHA-256/fingerprint is `dba943d448c092eff58dc4da3607ae83976e041984448df9a36425caa524fcfd` / `de458943b22b561c23025249f8ea45e7a9bdf97c273f667573e509b9a89f75dc`. A fresh bypass review found no material finding. Twenty-nine focused tests and the 1,626-test full suite pass with four skips; all other local gates pass. Gate: exact-HEAD correctness and security review, green CI, clean merge, then a separate reviewed one-use standing child before credential preflight or provider contact.

Program 012 runtime v4 update, September 1, 2026: exact-HEAD correctness review found that runtime v3 froze `f6d5d18e30de20d3b96aaaf567b4529a25974297` as the future child source before eight runtime-finalization files were committed. The existing preflight permits only the child and its review after the child's source, so the documented merge-then-child sequence could never activate. Runtime v4 preserves the v3 code and changes only the prospective lineage rule: the future child must freeze exact clean synchronized `main` after runtime v4 merges. A regression models runtime source, finalization, and child/review commits; it rejects the stale binding and accepts the post-finalization binding. V4 commit/tree/root is `47de7536ca8a85c3dd2c7c8523218751fd751a9c` / `7cf99cfee801370e7a5306019b2ca0a36f18166b` / `52f20b9d3e5a8e36b5b3ee0f03aeac2592577972e969a5362b0df9028b65635b`; artifact SHA-256/fingerprint is `117212f123d65784d9703b4529dba0dfb196fa64f9589a0d9d5b7fb58bf31a4f` / `8ec987889ec3fe5fb993f26d77eb25d17e1fbb0f452296ab7ad28346da014d27`. The 1,626-test full suite passes with four skips; all other repository gates pass. Gate: exact-HEAD correctness and security review, green CI, clean merge, then freeze and review the child against that exact main before credential preflight or provider contact.

Program 012 terminal update, September 2, 2026: the reviewed standing child activated on synchronized main `778e03e6b1a5cb1ecfbd242b39260a0983fcc347` and consumed its one use. The redacted public result is `RUNTIME-FAILURE` / `FAIL-CONSUMED-NO-RETRY`, with `admission_passed=false`, no dataset lineage, and no strategy calculation or return. Dynamic acquisition state, detailed failure evidence, private content identity, and market observations remain private under proposal v4. Current closeout code binds the exact public terminal semantics and revokes credential preflight, authority derivation, activation, loading, and execution before credential or private-root access. Program 012 cannot replay. Finding-free independent review artifact SHA-256/fingerprint is `abb99b2fc066b192455967c6a1fc51899d9ef3abeee49dbf91ef3821a71c3a1a` / `90565910623a1bba80c93c15d81525014a3e763e9c6fcef92b826359ad270993`. Gate: green CI and merge; then bounded offline forensics and a prospective successor only if scientifically justified.

Program 013 terminal update, September 4, 2026: `multi-hour-sector-etf-research-012` is `FAIL-CONSUMED-NO-RETRY`. The reviewed child sealed a redacted `RUNTIME-FAILURE` with `admission_passed=false`, no dataset lineage, no Program 002 admission, and no strategy calculation or return. Public terminal SHA-256 is `7e4d148b7a20122cdb5fde21f6f8d70493cfd5772a527aa42a3c127c067f56ee`; authority fingerprint is `b424096aabf9aaec0ad6282db9c8d5b291c405dde551377c584a3cbf063a9916`; source commit is `71b8a66dc8ea1f70673456f4bfe34561d05b6891`. Current closeout code requires the exact terminal and rejects every lifecycle entrypoint before credentials or private roots. Program 013 cannot replay or retry. Finding-free independent terminal review SHA-256/fingerprint is `ac22344cdf031ee3df7b5eeae0448887b2140603f9b0af8193a296e82238a010` / `37dfee69e0cd43a2588e397fbec22d59656de61d12cff189368862cab87195f4`. Gate: green CI and merge; then publish the redacted Program 014 forensic disposition and freeze a new reviewed successor without changing the inherited science.

Program 014 forensic update, September 4, 2026: read-only offline validation confirmed that Program 012 plus Program 013 retained evidence is an exact completed whole-session prefix followed by one intent-only page frontier with no body, receipt, HTTP status, parsed missingness, data-quality outcome, or later evidence. Program 013 remains immutable and cannot replay. The corrected redacted disposition binds the exact Program 013 scientific contract, child/review, runtime source, terminal, and inherited cumulative budget. Its SHA-256/fingerprint is `72f99f9fb3f46fc2b03e3c77085348085c2125652b57c9c7a4aa99a14d29366b` / `7e59cc4e3657df1d3a658943b0fcf35ab97b90a388c26fd32d8077ac03810afb`.

Program 014 design update, September 4, 2026: proposal v1 keeps Program 012 and Program 013 roots immutable, uses a distinct Program 014 root, reuses only completed whole sessions, discards the incomplete Program 013 session/page, and permits the frontier request once with zero retry. The unchanged 22,176 combined-intent ceiling allows at most 22,174 combined responses because both predecessors left one consumed intent without a response. Automatic restart, relaunch, `launchctl submit`, KeepAlive, restart-on-exit, and process-manager retry are prohibited. Proposal SHA-256/fingerprint is `52a4a523eed878530fe5112b72ddabc5b839c2bbd479825cd9fb18ce5c03e6f9` / `7d9a136af9e868a73cf18497ee2db323429046ae1573be3843b9f774f302b27d`. Gate: fresh finding-free design and security review, then implementation and a separate reviewed child. No credential, provider, acquisition, admission, strategy, controlled/protected, PAPER, broker, live, or purchase authority exists.

Program 014 design v2 update, September 4, 2026: proposal v1 failed independent review because
its terminal contracts were not exact recursive schemas and the secret guard scanned JSON
credential assignments one line at a time. Proposal v2 preserves all science, budgets, locks,
zero-retry rules, and disabled authority while adapting Program 013's exact private/public terminal
keys, branch values, assertions, pass-only control lineage, and missing/unknown-field rejection.
`observed_at` is the sole public closeout timestamp; market, session, and request dates remain
private. The whole-document JSON/JSONL credential scan and multiline regression are in place.
Failed-review SHA-256/fingerprint is
`3c8c5d53c657011905c0a49af3c34f0508a4b5116da8d95f42e02b790fc957ae` /
`8481bf093e4a35221d32e44b4b6cd319f4335d6a15ce6e0460b3c53e515c5698`; v2 proposal
SHA-256/fingerprint is
`3a6f4f1d78400a94e6a16bf43abcd6587a938bf9a309aacfd7028575f8f9c2d1` /
`fa86d332b58f182f0bd8488463a696268de0262ecfa82bb7b757ff00fa07283d`. Fresh review then found
that the private strategy work counts used booleans instead of integers and valid escaped JSON keys
could bypass the credential guard. V2 remains non-authorizing failed-review history.

Program 014 design v3 update, September 4, 2026 PDT (September 5 UTC): failed-review v2 SHA-256/fingerprint is
`13b0588e45e2e996087a21d7d8afc81c64368384f73f60f3e45024f0202f7e3b` /
`ff9e8b091ef598d922bd064c34c227dae47f915c65a4fa0f2dcd6ebffcef5d41`. Proposal v3 preserves
all v2 science and controls, fixes `strategy_calculations` and `strategy_returns` to exact integer
zero, and requires `type(value) is int` before a future runtime accepts either count. The shared
secret guard recursively checks decoded JSON and JSONL object keys, including escaped names, while
retaining raw-text detection for malformed documents. Escaped-key JSON and JSONL regressions pass.
V3 SHA-256/fingerprint is
`bdd2cdefd3f89f513fc19087d4a6191ad8e8cfbd891d77172de6736865425d1e` /
`e3969f650dd0bee166793c10c76e85dae81b25e9f4402dd02c2b1a1fc8e0a442`. Fresh independent design
and security reviews of source commit `07899b4846ad646f87640f3b385c41116f2c52cc` passed without findings.
Combined review SHA-256/fingerprint is
`c34bd327fd065dee1eb93f0c631f0df2dfa2f85d9cbb72195ff4c9da71b93e98` /
`a6c6720d40dfe020b5c4a02d3dc987bc0fd44d8e5ebf104e66efc43a5d8b9a1a`. Gate: green CI and merge;
then implement and independently review the exact runtime before any separate child, credential,
provider, acquisition, admission, or strategy action. Every current authority remains false.

Program 014 runtime update, September 5, 2026: the reviewed v3 runtime uses one lock-held
activation/execution transaction, holds Program 014, Program 013, Program 012, and Git controls
through terminal fsync, rejects restarted launcher state, enforces the 22,176-intent / 22,174-response
ceiling and exact integer-zero strategy counts, and leaves the public terminal digest for a separate
post-run closeout. Source commit/tree/root is
`e3c1e49e45c5c75c29feb050fb687ca4405ccc07` /
`0a66061a785617c1d0475dd54a9f26bf03adcc7b` /
`e5bff812bc2a47b349f56f8860ea350c61574bd209883e7edae88be8f463391c`; artifact
SHA-256/fingerprint is `c233f35850d709bca15d3abae2bbf8463d5da570f92afdcd5efe667fce55b147` /
`a79e7d4281b0265b3d7a2302f1910ca23d20366ba7338f3583cad731ecd58d2d`. Fresh independent
design/correctness and alternate defensive-boundary reviews passed without findings; dedicated
security-review attempts failed only through HTTP 503, policy-service rejection before output, and
HTTP 429. The full suite reports 1,759 passed and four skipped, and all other repository gates pass.
Gate: merge this exact runtime binding, freeze clean synchronized main, add only the exact child and
its independent review, then run names-only credential preflight. No credential value, provider,
private Program 014 state, acquisition, admission, or strategy action has occurred.

Program 014 child-authority update, September 5, 2026: the exact one-use child and its independent
finding-free review were added as the only two files after runtime-source commit
`818a923349ec13b18ec6dc1743e1f136f56bcc8c` and merged at `d81c09183ac3b3c48229205a9bf8f9034f6c69c9`.
The child enables only provider contact, credential access, source requests, market-data acquisition,
and real-dataset admission; all strategy, qualification, controlled, protected, PAPER, broker, live,
and purchase flags remain false. Child SHA-256/fingerprint is
`561299d05ac40f41513d92a3076f0eaf789c861f9a17d442a3d34e6ef2a54e2a` /
`3b46ea5689db84cee4e069628574a117c23a2374deea02b2bcde1f6a48b3a4b4`; review SHA-256/fingerprint is
`b946917095f89a4d4c858a9df10658eda093ff6e1b917a5fed62fc9ea22e2061` /
`57cf2f12c069e0a2dbae9f8a758b4e282ceca2ebb93acdc3fd6dc1631d9e8a07`. The names-only preflight then
reported `PROGRAM_006_ALPACA_API_KEY_ID` and `PROGRAM_006_ALPACA_API_SECRET_KEY` missing from the
current process. No credential value, provider request, private state, acquisition, admission, or
strategy action occurred. Gate: restore/export those existing variables, rerun the names-only preflight,
and proceed only if it passes; the missing credentials are the current human-attention boundary.

Intraday Autonomous Research 001 fixed three successor mechanisms, 90 specifications per campaign, 270 total, and no Campaign 4. Campaign 1 completed 18 discovery runs and remained empty after an independently reviewed read-only reassessment. Campaign 2 completed 18 discovery runs; four parents had positive Normal and zero-cost returns, but every parent failed activity and participation-bucket concentration. Campaign 3 launched once from exact main `e74c5632529ef568b043428a251a1014e4f443de`. All 18 discovery specifications failed once at the inherited per-run plan-container boundary before bounded bar loading, strategy execution, or return observation. Campaign 3 is terminal and its Fed-policy hypothesis is unassessed. The program consumed 54 specifications and has no permitted campaign capacity. No serious cohort, controlled-qualified candidate, or intraday PAPER-ready candidate exists. Remaining infrastructure work includes root-cause evidence for the Exposed 005 lease-loss episodes and resumable long runs where needed. Gate: all attempted candidates and failures remain recorded; unsupported task envelopes fail closed; agents cannot alter protected controls or promote strategies.

Campaign 3 terminal update: one exact four-worker launch from clean main
`e74c5632529ef568b043428a251a1014e4f443de` reserved and attempted all 18 discovery specifications.
Every run failed once at the inherited per-run plan-container boundary. Permitted exposed artifacts
were integrity-validated, but bounded bar loading, strategy execution, canonical report publication,
and return observation did not occur. The frozen repair exception no longer applies after reservation
and attempt. Campaign 3 cannot be repaired or relaunched; Campaign 4 is prohibited. The bounded
program is closed for user strategic review with two discovery rejections and one unassessed campaign.
Finding-free independent closeout review and immutable state revision 8 bind that disposition.

## Intraday extension

- M5A — Intraday data and replay foundation: immutable `1m` and `5m` SPY/QQQ bars, XNYS regular-session and early-close validation, explicit bar-open timestamps, missing-interval evidence, read-only historical acquisition, deterministic fixtures, and conservative next-bar replay. Implemented. The first real Campaign V1 import exposed and stopped on an Alpaca extended-hours filtering defect. The corrected adapter retains every mapped transport bar as raw evidence and admits only exact requested XNYS bar opens to normalization; validation was not weakened. V1 ran no candidate; V2 later completed its controlled matrix on corrected immutable data.
- M5B — Intraday research and qualification foundation: V1/V2 `intraday-experiment-v1`, exact-range controlled training and validation, `intraday-backtest-report-v1`, fixed baselines, flat-at-close evidence, fixed search accounting, and research-only gates remain implemented. Campaign V1 stopped before candidate execution. Campaign V2 completed 60/60 candidates, failed 12/12 qualification groups, and opened no holdout. V3 remains sealed with 60 pending reservations for three future validation blocks. Exposed 002 closed terminally interrupted; Exposed 003 closed materially incomplete; Exposed 004 stopped before a strategy attempt; Exposed 005 completed 272 runs and froze empty. Four later event campaigns closed empty. Autonomous Campaigns 1 and 2 completed 18 discovery runs each and froze empty after independent review. Autonomous Campaign 3 consumed 18 discovery specifications in one terminal data-boundary failure before strategy execution and remains scientifically unassessed. Intraday Autonomous Research 001 is closed with no serious cohort, controlled-qualified candidate, or PAPER-ready candidate. June remains ineligible and no substitute range is allowed. Protected intraday holdout access, paper execution, and milestone completion remain pending separate review.
- Campaign 3 terminal update: all 18 discovery specifications failed once before bounded bar loading
  or strategy execution. No canonical run report, return, freeze, or cohort exists. Campaign 3 is
  terminal and unassessed; no repair, relaunch, Campaign 4, controlled evaluation, or PAPER authority
  is permitted.
- M5C — Intraday paper execution: reviewed market-data freshness, risk, order, reconciliation, operational, threat-model, and authority controls after extended qualification. Not started; no intraday broker-write authority exists.

## M7 — Controlled continuous improvement

Scheduled sealing and reevaluation, champion/challenger lifecycle, shadow-to-paper advancement, drift detection, rollback, and reviewed promotion reports. Gate: every transition is explicit, reversible, evidenced, and human-approved.

## M8 — Future live canary

Out of scope. Gate to begin planning: qualified extended paper results, explicit user approval, reviewed live-risk policy and threat model, independent kill-switch validation, capital limits, legal and broker review, runbooks, and recovery drills.
