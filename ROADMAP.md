# Roadmap

Each milestone requires passing formatting, lint, typing, tests, secret checks, documentation review, and every listed gate. A later milestone does not weaken an earlier gate.

## Immediate research direction

1. Use Rapid Research for fast historical daily strategy development on exposed data.
2. Add and reject broader strategy families through bounded, fully recorded sweeps.
3. Require chronological walk-forward and worse-execution stress before candidate export.
4. Package promising results as zero-authority paper-candidate inputs for separate controlled review.
5. Evaluate only qualified, independently authorized candidates in the guarded paper system.

Research iteration is cheap; promotion and execution remain strict. V3 stays separate: its exact main seal was attested before Validation A, and the campaign has been materialized with 60 pending reservations. It now awaits its future validation periods and does not block ordinary historical research. Live trading remains out of scope.

## M0 — Foundation and policy

Package, safe configuration, CLI, CI, persistent documentation, initial domain types, deterministic fingerprints, storage layout, and test infrastructure. Gate: clean setup; CLI and checks pass; runtime defaults offline; live is rejected; no secrets or generated data are tracked.

## M1 — Market data

Provider-neutral acquisition, Alpaca historical adapter, fixtures, immutable raw and normalized storage, catalog, validation and quarantine, calendar and corporate-action policy, and data CLI. Gate: adjusted daily data for the fixed universe is reproducible; provider corrections create versions; invalid data cannot silently enter a dataset; catalog reconstruction works. The fixed-universe gate now covers XNYS calendar validation, raw JSONL and Parquet artifacts, deterministic fixtures, read-only paginated Alpaca acquisition, immutable correction lineage, cross-provider version isolation, provider-adjusted action handling, rejection of unadjusted data, and issuer-sourced point-in-time membership bound into dataset and experiment provenance. A local unadjusted corporate-action processor remains out of scope for this slice.

## M2 — Backtester and baselines

Timestamp semantics, portfolio accounting, deterministic orders and fills, conservative versioned costs, ledgers, metrics, benchmarks, and simple baseline strategies. The current slice covers explicit next-bar fills, cash accounting, bps slippage and commission, decision/order/trade/equity ledgers, cash, buy-and-hold, fixed-weight, moving-average trend, time-series momentum, moving-average mean-reversion, and volatility-targeted exposure baselines, cash-relative report comparisons, deterministic fixture reports, and a separate complete-session boundary for atomic multi-symbol portfolio targets. Gate: no lookahead; accounting and order invariants pass; fixed transactions cannot benefit from higher costs; reports reproduce. Walk-forward evaluation and qualification remain M3.

## M3 — Experiments and qualification

Durable campaigns and experiments, restart recovery, split controls, walk-forward and robustness checks, comparisons, gate-based qualification, and protected holdout workflow. The current slice covers search budgets, pending/running/completed/failed lifecycle, heartbeats and stale-run recovery, explicit split classification, hidden holdout metrics with logged access events, approved disqualifying gates, range-limited registry-backed runs on cataloged datasets, chronological walk-forward candidates, cost and delayed-fill sensitivity candidates, bounded run and comparison CLI commands, score-free comparison reports, fingerprinted training plans that preregister exact candidates before execution, and controlled-run provenance with fingerprinted report artifacts for qualification sources. Rapid-002 adds one candidate-specific validation-plan schema that atomically reserves 28 exact records and derives every run input from stored state. Its 28 records completed, but controlled qualification rejected the candidate for zero fixed-weight benchmark wins and excess instrument profit concentration; no independent range opened. Provider campaigns retain superseded methods and corrected reruns. Report schema v2 adds risk-adjusted return, exposure, session and instrument concentration, and SPY regime metrics. Recorded training campaigns include rejected long-horizon per-symbol momentum and rejected monthly relative-strength portfolio families with predeclared parameter neighborhoods. The approved machine-readable policy records every gate and rationale. A registry-backed evidence manifest reproduces aggregate metrics and formal rejections in an immutable report without loading bars. One-time stored authorization replaces the raw holdout-creation flag and binds an exact qualified candidate to one post-validation holdout record. The controlled runner consumes that authority before reading data, scans only the authorized Parquet timestamp range, retains failures, and keeps completed metrics behind the separate read event. Strategic allocation passed all approved validation and one-time holdout gates. Gate: failures remain evidence; ordinary research cannot read holdout results; no single score can hide a disqualifying failure.

## M4 — Alpaca paper execution

Paper-only adapter, intents, independent risk, order management, idempotency, broker events, reconciliation, operational journal, and emergency disable. The reviewed design requires one transactional execution database, deterministic client order IDs, exact paper-endpoint selection, fresh broker and market snapshots, forward-only state, reconciliation before retry, persistent emergency disable, and append-only hash-chain evidence. Broker-free intents, journal verification, risk gates with no default values, default-on emergency disable, code-bound paper authorization, durable fail-closed decisions, normalized snapshots, explicit flat baselines, durable reconciliation evidence, fixed-origin GET-only paper account state, journaled adapter provenance, stable emergency-clear readiness, idempotent journaled emergency transitions, authorization-bound strategy-capital baselines, immutable fill-and-bid-marked strategy-equity checkpoints, complete attested risk context, transaction-bound attested risk decisions, atomic settled positive-fill capacity release, transaction-bound quantity-order submission preflight, one-shot fake submission outcomes, strict paper order POST normalization, durable one-shot cancellation attempts with unknown-outcome evidence, strict single-order DELETE normalization, immutable deterministic cancel-all order-set planning, restart-safe per-order plan consumption, immutable positive exact-lookup provenance, read-only terminal cancellation recovery, a fail-closed paper-write readiness runbook, dormant activation, opt-in, assessment, and revocation controls, exact activation-bound submission and cancellation attempt caps, a main-only attested-wheel build path, fixed-authority wheel and manifest verification, exact non-editable installed-distribution binding, transaction-bound runtime identity evidence, a fixed-origin production mutation transport, and an activation-bound production coordinator now exist. Exact paper process opt-in opens only the outer runtime gate; transaction-bound controls remain authoritative. Gate: unknown or stale state blocks writes; duplicate intents are harmless; reconciliation discrepancies stop execution; live endpoints cannot be selected.

M4 also includes a bounded unchanged-terminal replay recovery proof and explicit, idempotent local
schema initialization. The first real Alpaca paper session ran on 2026-08-04 and filled four
production-bound orders: GLD 3, IWM 8, QQQ 3, and SPY 4. Its initial-entry replay/shadow/paper
comparison passed. Present-state planning and an append-only non-flat authorization handoff now
support a later strategic-allocation session without resetting positions, fill economics, equity
peak, drawdown, or the root strategy epoch. Planning derives the inclusive XNYS session count from
the root fill-backed and current attested NYSE clocks and derives its market-state fingerprint from
immutable current evidence; neither value is an operator input. Declaration, completion, and
planning add no broker authority. The current Alpaca HTTP 401 remains an operator credential
blocker, and authority-grade verification still requires the unprivileged deployment account.

## M5 — Equivalence and sustained paper operation

Replay/shadow/paper comparison, supervisor recovery, disconnect and stale-data handling, and sustained paper campaign. The current slices add immutable broker-read-only campaigns bound to production-attested baseline positions, explicit healthy, drift, read-failure, stale, historical-continuity, and final-campaign states, CLI sampling, immutable action-plan comparisons across replay, shadow, and stored paper intents, and boot-enabled bounded systemd supervision of the same observation loop. Neither path has broker-write authority. Week 1 completed healthy with no drift and one recovered read failure, but its approximately 1030.755-second maximum gap, reported as 1031 seconds, exceeded the fixed 900-second limit during two VPS reboots. The formal operating-limit gate did not pass; the reboot cause explains but does not erase the failure. A later shorter reboot/recovery validation passed, and its longer follow-up remained healthy until it was stopped. The remaining 168-hour duration was explicitly waived; the unobserved period remains an evidence gap. Gate for any future sustained-paper claim: discrepancies are explained, recovery drills pass, and operating limits hold for the full period required by that decision.

## M6 — Bounded automated research

Rapid Research provides bounded deterministic sweeps, chronological walk-forward folds, execution stress, isolated result storage, and zero-authority export. A spawned-process executor supports bounded same-stage work with four workers by default and the unchanged `research-attempts-v1` retry boundary. Exposed 005 completed 272 runs, recovered 12 lease-expired attempts, and froze empty; its deterministic fixture benchmark remains the valid throughput measure at 3.426x. Event Drift, Event Repricing, Event Opening Breakout, and Event Prior-Low Rejection later closed empty under their frozen discovery gates.

Program 002 implementation is complete for `multi-hour-sector-etf-research-001`: twelve traded ETFs plus SPY context, two fixed cross-sectional families, eight configurations, one 11:30 New York decision, 232 maximum specifications, and 696 maximum attempts. The exact 20,280-row context dataset remains immutable and Controlled A/B remain unacquired. Authority v6 attempt `program-002-exposed-acquisition-20260826-v5` reused that context, created its volume projection, and froze seven Exposed Block 1 monthly segments through January 2021. Complete February pagination then returned 19,259 of 19,266 required normalized rows. Seven MDY coordinates were absent, including one consecutive pair, so both the foreign-coordinate and consecutive-gap stop conditions apply. Corrected incident v3 SHA-256/fingerprint is `ebc8f536d51fd701f9fc26f28630abff9f23a0b5edb3e794a5a3016337273618` / `1f5350eb3f704c343044cd11ac328643fd1ae0939ebeaa030248ce031938b53a`; finding-free review v2 SHA-256/fingerprint is `18ee95730f5af8698ff090c74df42774e1294dea8ff5552ad6c3b16495566e53` / `23c20fb78b71c3e527eb5c4f916d58463fdb311daf2a66436c5d32f976047cbf`.

Authority v6 remains revoked at source commit `0ee88843ff56a1a580475e4608e3e82b66123841`. The later one-use minute-source authority ran exactly once from clean main `589394e01142879c00112e4cf84d7783f492bd30`. Four exact Alpaca SIP `1Min` requests returned 1,168 rows in four HTTP 200 terminal pages. Source-proof reload exposed a `Decimal`-to-string representation mismatch, but independent structural aggregation found 305 exact frozen-control matches and exactly seven empty buckets: the seven authorized targets. This source cannot reconstruct a target, so no processing repair or retry is permitted. Failure SHA-256/fingerprint is `f95850c120603608f616f2e88d579424f4dc80301aa1251b7188fcca89fc08cf` / `b15a5acbce63758fe40619db1861f6e624d454651d9977fcddbda269b92cca2e`. Revocation commit `49bb011b69327b97fbee74e0862a60c0d58600ec` makes every data home exit 64 before comparator loading, claims, credentials, client construction, or transport; finding-free closeout-review SHA-256/fingerprint is `90f9f0a64a73951001846b305ff0927a249369224c8e7d03e4c2c30cfd599ae7` / `e887f347b37c8ecc01506e876ec893e0eae56442f5f2fd250328323f145d8707`. No source proof, canonical admission, exposed dataset, quote evidence, cost model, Campaign 1 binding, or real strategy result exists. Strategy execution, controlled/protected access, qualification, PAPER, broker writes, and live execution remain false. The next gate is separate user authorization for a prospective plan that selects a different source or an explicit independently reviewed missing-data disposition.

The prospective missing-data disposition now freezes whole-session exclusion, 100% coverage of all twelve ranked ETFs plus SPY, 57-of-60 coverage in every preselected quote window, one-session and one-percent fixed-period loss ceilings, one incomplete or same-symbol-incomplete session per rolling twenty sessions, and zero required-context loss. Its public boundary binds the exact reviewed 1,531-session exposed table and 657-window quote grid and exposes no schedule or policy override. Finding-free independent-review SHA-256/fingerprint is `7b23457dced43c78e8925ac5dad9d75da77feae7018ce91551c26a86d9df1372` / `d07ddce0ccb87770e458fafb2ff08f7d69a95f32ae70ea8f0cd91d251103bdd0`. The known MDY-only February gaps fail the disposition, so the attempted source remains inadmissible and a separately authorized different-source planning phase is the next scientifically acceptable path. No acquisition or execution authority is added.

The Massive qualification boundary is now implemented and mock-validated for the exact 630-chain sample, but all three mandatory pre-transport gates fail. Exact adjustment and provider aggregate-eligibility semantics are unproved, and public terms do not grant the required retention and derived-research rights. No credential, authority, provider request, real qualification evidence, dataset admission, or strategy result exists. Program 002 stays stopped. The next gate is executed written contractual evidence plus provider-authored semantic bytes, followed by a new independent gate review; full acquisition remains outside this phase.

Program 003, `multi-hour-sector-etf-research-002`, is proposed as a prospective low-cost successor without reopening Program 002. It binds the exact predecessor plan and keeps the same untested eight strategy configurations, chronology, campaign gates, and protected controlled-evaluation sequence while replacing historical NBBO with a universal 6/12/25-basis-point-per-side envelope and 5/10/15-minute delays. The sole source candidate is Tiingo's Beta consolidated historical intraday endpoint; raw unadjusted bars remain source evidence, split normalization is repository-owned, and missing data excludes the whole thirteen-symbol session under return-blind global and concentration limits. The fixed source qualification allows 221 GET chains and responses but has no authority. Current Tiingo terms prohibit durable Starter persistence and require a continuing $30 Power subscription while data is retained; required bar and corporate-action semantics also remain unresolved. The exact plan and finding-free review fingerprints are `f5b184ff3e1604a151a82214d1cf91fbdffa6fc4fddf7d7ce0506a2e99427a42` and `55ea3dcb3fc122034b59909bd8431886aa61fac5e8c83ea2a53eb9fdbd060bdb`. The next gate is a user decision on licensed retention plus provider-authored semantics, followed by implementation, synthetic verification, independent review, and a separate one-use qualification authority. No market-data request or strategy execution is authorized.

Program 004, `multi-hour-sector-etf-research-003`, prospectively replaces only Program 003's Tiingo data and provenance architecture. MarketParquet's current standard license supports the needed private perpetual retention and one-time archive model; native five-minute, split-adjusted, non-dividend-adjusted ETF files are mathematically compatible with the inherited same-session strategy under fractional-share and bps-on-notional accounting. The exact plan and finding-free review fingerprints are `2b740810f1365d20c11d1cf3bb83cdd0e06e72c30a5070ff0648031f29d893fc` and `5343b70c5cfe9e6c4342286cb6e1bac9c75b8344b620ce1110d69f90ec99efd0`. The later full range contains 1,531 XNYS date files: twenty context and 1,511 exposed evaluation sessions, with at most 1,516 additional downloads after qualification reuse. The next possible gate after green CI, merge, and a new user grant is only a capped manual Complete Archive purchase and fifteen-file structural qualification. Full exposed acquisition and every strategy stage remain separately unauthorized.

Program 005, `multi-hour-sector-etf-research-004`, abandons the paid Program 004 path before purchase and prospectively selects free Alpaca Basic historical SIP. Official pages retrieved August 27, 2026 confirm the zero-dollar plan, history since 2016, SIP access for an explicit end at least fifteen minutes old, native five-minute bars, inclusive bounds, token pagination, adjustments, and 200 requests per minute. Raw and paired `split,spin-off` views preserve source evidence, raw fills, and cross-session price/volume comparability. The exact `7/1499` global loss ceiling comes from Program 003's independently reviewed source-neutral Tiingo plan, which expressly recorded that the known MDY pattern failed its old concentration controls. Five MDY dates form one immutable pre-exposed design quarantine and leave two isolated unexpected slots. Its hash-bound complete Program 002 incident inventory, discovery-block, month, year, five-minute-clock, strategy-clock, nonadjacency, and six SPY/MDY morning-tail gates are immutable; the class cannot be subsetted, expanded, or reused, while inherited recurrence controls govern every future unexpected exclusion. The one-use qualification freezes thirteen inclusive ranges, twenty-six paired chain identities, per-chain two/six-page caps, 28 expected and 60 maximum responses, 64 MiB, and reuse without resegmentation. Its sample contains no realized spin-off, so every later in-range spin-off remains admission-blocked until issuer/exchange evidence and exact paired-factor validation pass. Hash-bound issuer pages record current 0-2-bps 30-day median full spreads for all traded ETFs. Corrected plan SHA-256/fingerprint is `3a71573086418aa8ff53d8359110dee1a951caa333ffd35eccedd8d38678cb11` / `79a73d143700c643d67c2f862b5bfe3655df9706276a1b70e189c425d4397cb7`; public-contract evidence fingerprint is `bb389757b60777cc20549c58201b71f130151dfb1ec65d2959ec1b082f911c2e`; finding-free independent-review SHA-256/fingerprint is `6155632a474351084d7a8b6670dde0ddf30f7cca3d6bd77abad9f4d5546c493e` / `276e08b440012739d36d666c05cec2ba6421f4e381c6b148752fb2c7682960e6`. All fourteen required challenges are addressed, thirteen focused tests pass, and challenge 11's material licensing ambiguity remains an authority failure. The customer, Nasdaq, and NYSE agreements do not expressly grant private immutable retention, so the next gate is written contractual resolution without a paid plan, followed by a reviewed provider adapter and separate one-use structural qualification authority. Executable mocks add no request capability. No provider contact, credential, request, acquisition, strategy return, or protected authority exists.

Program 005 implementation update, August 28, 2026: the user's private-retention direction resolves the planning gate for private personal research while public redistribution stays prohibited. The public recipe now includes the exact adapter, private create-only page and dataset storage, deterministic paired views, action and missingness admission, fixed-quarantine recomputation, credential isolation, repository leakage checks, and a credential-free CLI preflight. The implementation-bound proposal is non-active with every authority flag false, and its fresh closure review is finding-free. Qualification execution still requires clean synchronized main and separate user authorization for that exact one-use scope. Full execution remains blocked pending exact qualification-byte, receipt, and review bindings. No credential was loaded, no provider request was made, no observation or strategy return was generated, and every protected, PAPER, broker-write, and live flag remains false.

Program 005 activation update, August 28, 2026: the separately authorized attempt stopped before credentials or authority consumption. Fresh review rejected a candidate whose source changed one proposal-bound file, then independent validation showed that the active-authority loader accepts re-fingerprinted changes to claimed user-authorization and review provenance. The request plan and one-use runtime boundary remain bounded, but the exact authorization contract is not enforced. The candidate was discarded. Review SHA-256/fingerprint is `f7df27d39c8bbef5134b7238981571e660194de26486fbd7a70d177636743df8` / `76314643d8dd1b6f88620c8340a58bbd1847c70dcc377836671cd0feac9f7c11`. No active authority, private root, claim, credential load, provider request, observation, or strategy return exists; all protected authorities remain false. Gate: repair the loader, issue and independently review a new proposal, merge through green CI, and obtain new exact user authorization. The current authorization packet cannot transfer to changed bytes.

Program 005 authority-binding repair update, August 28, 2026: authority v2 is mechanically derived from exact proposal, implementation review, proposal review, scientific scope, authority flags, and a ten-file implementation root. Runtime requires the external user-authorized root, fixed ignored authority path, clean synchronized main, immutable control-artifact lineage, unchanged reviewed source bytes, and a second full validation under the one-use lock. Implementation commit/root is `928e6bb797be987d5e37c180a23fdd5f2476b603` / `82659ef32ddc00968bd588e24de1677d51788de23edca321cab93f37750f7114`; finding-free implementation-review SHA-256/fingerprint is `041d7926652663a0382ea960d7bb4b3085febb83d6d17f4ae3d6822928ba79b0` / `60b2eaa62d84e36b716fdd3b52943cfa60c4f27fff97d0686ff83f477e6a93a8`. Stage A merged as `3bdc2087f3a0e965d6d64fd24e0de22a98ba27b0`. The inactive v2 proposal `program-005-source-qualification-authority-proposal-2026-08-28-v2` binds that merge with SHA-256/fingerprint `c3ebe6c5ca36cba26468fe75af952b0f8019188660840b287e7503e945335b00` / `e74b8131cf82646b56b2609e2c9de077cebe25993a2dcec7bcac6bb4735b2e6b`; its fresh finding-free review SHA-256/fingerprint is `ff17381bac27ec3e049650fdfd5471e6f13c3c925ec474b01ff259fa0e8f6c53` / `f72387b3960223945de3ec37d5e93ed61ec3da1c01e45a0f9f01d5bd7f682923`. Credential-free preflight passed 22 sessions, 13 symbols, 13 ranges, 26 chains, 28 expected and 60 maximum responses, 64 MiB, 120 requests per minute, one future credential load, and zero retries. Stage B must pass green CI and merge on clean synchronized main before the exact root can be reported for separate user authorization. No active authority, credential access, provider request, qualification, private data, strategy return, or protected action exists.

Intraday Autonomous Research 001 fixed three successor mechanisms, 90 specifications per campaign, 270 total, and no Campaign 4. Campaign 1 completed 18 discovery runs and remained empty after an independently reviewed read-only reassessment. Campaign 2 completed 18 discovery runs; four parents had positive Normal and zero-cost returns, but every parent failed activity and participation-bucket concentration. Campaign 3 launched once from exact main `e74c5632529ef568b043428a251a1014e4f443de`. All 18 discovery specifications failed once at the inherited per-run plan-container boundary before bounded bar loading, strategy execution, or return observation. Campaign 3 is terminal and its Fed-policy hypothesis is unassessed. The program consumed 54 specifications and has no permitted campaign capacity. No serious cohort, controlled-qualified candidate, or intraday PAPER-ready candidate exists. Remaining infrastructure work includes root-cause evidence for the Exposed 005 lease-loss episodes and resumable long runs where needed. Gate: all attempted candidates and failures remain recorded; unsupported task envelopes fail closed; agents cannot alter protected controls or promote strategies.

Campaign 3 terminal update: one exact four-worker launch from clean main
`e74c5632529ef568b043428a251a1014e4f443de` reserved and attempted all 18 discovery specifications.
Every run failed once at the inherited per-run plan-container boundary. Permitted exposed artifacts
were integrity-validated, but bounded bar loading, strategy execution, canonical report publication,
and return observation did not occur. The frozen repair exception no longer applies after reservation
and attempt. Campaign 3 cannot be repaired or relaunched; Campaign 4 is prohibited. The bounded
program is closed for user strategic review with two discovery rejections and one unassessed campaign.
Finding-free independent closeout review and immutable state revision 8 bind that disposition.

## Intraday extension

- M5A — Intraday data and replay foundation: immutable `1m` and `5m` SPY/QQQ bars, XNYS regular-session and early-close validation, explicit bar-open timestamps, missing-interval evidence, read-only historical acquisition, deterministic fixtures, and conservative next-bar replay. Implemented. The first real Campaign V1 import exposed and stopped on an Alpaca extended-hours filtering defect. The corrected adapter retains every mapped transport bar as raw evidence and admits only exact requested XNYS bar opens to normalization; validation was not weakened. V1 ran no candidate; V2 later completed its controlled matrix on corrected immutable data.
- M5B — Intraday research and qualification foundation: V1/V2 `intraday-experiment-v1`, exact-range controlled training and validation, `intraday-backtest-report-v1`, fixed baselines, flat-at-close evidence, fixed search accounting, and research-only gates remain implemented. Campaign V1 stopped before candidate execution. Campaign V2 completed 60/60 candidates, failed 12/12 qualification groups, and opened no holdout. V3 remains sealed with 60 pending reservations for three future validation blocks. Exposed 002 closed terminally interrupted; Exposed 003 closed materially incomplete; Exposed 004 stopped before a strategy attempt; Exposed 005 completed 272 runs and froze empty. Four later event campaigns closed empty. Autonomous Campaigns 1 and 2 completed 18 discovery runs each and froze empty after independent review. Autonomous Campaign 3 consumed 18 discovery specifications in one terminal data-boundary failure before strategy execution and remains scientifically unassessed. Intraday Autonomous Research 001 is closed with no serious cohort, controlled-qualified candidate, or PAPER-ready candidate. June remains ineligible and no substitute range is allowed. Protected intraday holdout access, paper execution, and milestone completion remain pending separate review.
- Campaign 3 terminal update: all 18 discovery specifications failed once before bounded bar loading
  or strategy execution. No canonical run report, return, freeze, or cohort exists. Campaign 3 is
  terminal and unassessed; no repair, relaunch, Campaign 4, controlled evaluation, or PAPER authority
  is permitted.
- M5C — Intraday paper execution: reviewed market-data freshness, risk, order, reconciliation, operational, threat-model, and authority controls after extended qualification. Not started; no intraday broker-write authority exists.

## M7 — Controlled continuous improvement

Scheduled sealing and reevaluation, champion/challenger lifecycle, shadow-to-paper advancement, drift detection, rollback, and reviewed promotion reports. Gate: every transition is explicit, reversible, evidenced, and human-approved.

## M8 — Future live canary

Out of scope. Gate to begin planning: qualified extended paper results, explicit user approval, reviewed live-risk policy and threat model, independent kill-switch validation, capital limits, legal and broker review, runbooks, and recovery drills.
